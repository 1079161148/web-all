---
description: 代码评审清单（手动触发，用 @code-review-checklist 引用）。提交 PR 前自查、或评审他人代码时应用，覆盖架构红线、多租户、权限安全、性能、测试、文档六个维度。
alwaysApply: false
enabled: true
provider:
---

# 代码评审清单

> 使用方式：提交 PR 前自查，或在对话中用 `@code-review-checklist` 触发评审。

---

## 0. 先判断复杂度级别

| 级别 | 适用 | 评审强度 |
|---|---|---|
| **L1 简单模式** | 支撑域 CRUD | 走 §2 / §5 / §6 关键项 |
| **L2 标准模式** | 常规业务模块 | 全清单 |
| **L3 严格模式** | 核心域（租户 / 权限 / 计费 / 流程） | 全清单 + **架构组 review** |

**L3 变更必须由架构组评审**，不得自行合并。

---

## 1. 架构红线（最高优先级）

- [ ] `admin-domain` **无任何框架依赖**（无 `org.springframework.*` / `com.baomidou.*` / `jakarta.persistence.*`）
- [ ] 依赖方向正确：`interfaces → application → domain ← infrastructure`
- [ ] `application` 未直接依赖 `infrastructure`
- [ ] 聚合根**没有** `@TableName` / `@Service` 等框架注解
- [ ] 领域对象 / PO / DTO **三层分离**，转换用 MapStruct
- [ ] **AppService 中没有业务分支 `if`**（规则应在聚合根 / 值对象 / 领域服务）
- [ ] 跨聚合只持 **ID 引用**，未持对象引用
- [ ] 未直接跨限界上下文引用对方的 Repository / Entity
- [ ] `ArchUnit` / `ApplicationModules.verify()` 通过

---

## 2. 多租户（L2 / L3 必查）⚠️

- [ ] 新表有 `tenant_id` 列
- [ ] **所有唯一索引都带了 `tenant_id`**
- [ ] 缓存 Key 带 `{app}:{tenantId}:` 前缀
- [ ] 文件路径带租户目录，下载有路径校验
- [ ] **异步 / 定时 / MQ 场景显式传递了租户上下文**（TTL 包装 / `ScopedValue` / 消息体字段）
- [ ] 按 ID 查询带了 `tenant_id` 条件
- [ ] 新增的忽略租户拦截的表**已在忽略名单显式声明**
- [ ] 补充了多租户隔离集成测试

---

## 3. 权限与安全

- [ ] **新增写接口加了权限注解**（`@PreAuthorize`）
- [ ] 前端按钮权限用 `v-permission`，**后端独立鉴权**
- [ ] 数据权限注解 `@DataScope` 已按需添加
- [ ] 敏感字段加了字段权限注解 / 脱敏（**导出与日志同样生效**）
- [ ] 参数校验完整（Jakarta Validation）
- [ ] 无 `${}` 拼接 SQL
- [ ] 文件上传有白名单 + MIME + 魔数校验
- [ ] 日志/异常响应**未泄露敏感信息**
- [ ] 无硬编码密钥 / 密码 / 内网地址
- [ ] 令牌/认证相关变更**未自研**，基于 Authorization Server

---

## 4. 数据库

- [ ] 变更通过 **Flyway 新增脚本**（未修改已上线脚本）
- [ ] 脚本有明确描述，DDL 与 DML 分离
- [ ] 表包含完整通用字段（`tenant_id` / `create_by` / `create_time` / `update_by` / `update_time` / `del_flag` / `version`）
- [ ] **字段注释完整**
- [ ] 索引合理：联合索引最左前缀、单表 ≤ 5 个、无冗余索引
- [ ] 未使用自增主键；金额用 `DECIMAL`
- [ ] 大表变更（加列/回填）**分批执行**
- [ ] 关键查询用真实数据量级验证过

---

## 5. 性能

- [ ] 新增接口关注 P99 与慢 SQL
- [ ] 无 `SELECT *`、无无 `LIMIT` 全表查询
- [ ] 无循环内查询（N+1）；批量操作分批
- [ ] 列表接口未返回大字段
- [ ] 前端无循环内发请求；多请求已并行或批量
- [ ] 表格单元格未使用组件库组件
- [ ] 新依赖已评估产物体积，未超预算
- [ ] 事务短小，未在事务内调外部 HTTP / 发消息

---

## 6. 测试与文档

- [ ] 核心域（L3）有单元测试，含**非法状态流转断言**
- [ ] 多租户 / 权限变更补齐了集成测试
- [ ] 未使用 H2 替代 Testcontainers
- [ ] 无长期 `@Disabled` 测试（或已建 issue）
- [ ] 新增 Pro 组件**同时提交 Histoire story + 文档**
- [ ] 接口变更**已更新 OpenAPI 注解**（description / 必填性 / 枚举）
- [ ] `pnpm gen:api` 生成产物已提交（`git diff` 为空）
- [ ] 破坏性变更已通过 `openapi-diff` 门禁并在 CHANGELOG 记录
- [ ] 涉及决策的变更已记录 ADR

---

## 7. 代码质量

- [ ] 命名符合规范（无拼音、无 `data1`、无无意义缩写）
- [ ] 异常：业务异常用 `BizException`，未吞异常，未返回异常栈
- [ ] 日志：用 SLF4J 占位符，无 `System.out` / `console.log`，无敏感信息
- [ ] 注释解释「为什么」，无被注释掉的死代码
- [ ] 无 `any` / `@SuppressWarnings` 泛滥
- [ ] 无"以后可能用到"的过度抽象
- [ ] 未修改无关文件；未提交临时调试代码
- [ ] lint / 测试 / 架构校验全部通过（未跳过 hooks）

---

## 8. 前端专项

- [ ] `apps/**` **未直接 import** `naive-ui` / `vxe-table` / `form-create`
- [ ] 未手写接口类型与请求函数（用了 `@admin/api` 生成产物）
- [ ] **服务端数据未放入 Pinia**（用了 TanStack Query）
- [ ] 未硬编码颜色 / 间距 / 字号（走 Token）
- [ ] 菜单未硬编码（由后端菜单树驱动）
- [ ] 状态矩阵完整：加载 / 空 / 错误 / 无权限 / 部分失败
- [ ] 空态有可操作引导，错误态有重试与 traceId
- [ ] 危险操作有二次确认或撤销
- [ ] 表单校验规则来自 zod（与后端一致），未手写一套
- [ ] 无 `useMessage` 在 provider 外调用

---

## 9. 结论档位

| 档位 | 含义 | 处理 |
|---|---|---|
| ✅ **通过** | 无以上问题 | 合并 |
| 🟡 **有条件通过** | 仅代码质量类问题 | 修复后合并 |
| 🔴 **打回** | 命中 §1 架构红线 或 §2 多租户任一项 | **必须修复，不得合并** |
