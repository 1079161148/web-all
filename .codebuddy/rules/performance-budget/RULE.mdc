---
description: 性能规范与预算：前后端量化性能指标、CI 性能回归门禁、首屏与分包策略、大表格极限场景、接口瀑布治理、数据库查询性能、虚拟线程与连接池约束。涉及性能优化、引入重依赖、写列表页或复杂查询时应用。
alwaysApply: false
enabled: true
provider:
---

# 性能规范与预算

> **没有数字的"性能高"是自我感觉良好。** 下表是硬指标，必须进 CI 门禁。

## 1. 性能预算表

### 前端

| 指标 | 目标 | 门禁 |
|---|---|---|
| 首屏 LCP（4G / 中端机） | **< 1.8s** | Lighthouse CI |
| INP | **< 200ms** | 线上 RUM（web-vitals） |
| CLS | **< 0.1** | Lighthouse CI |
| 首屏 JS（gzip） | **< 250KB** | bundlesize |
| 单个路由 chunk（gzip） | **< 150KB** | bundlesize |
| 表格 1 万行首渲染 | **< 500ms** | Vitest bench |
| 1000 行 × 10 列（含组件单元格） | **< 300ms** | Vitest bench |
| 全量构建耗时 | **< 90s** | CI |
| 增量构建 | **< 15s** | CI |

### 后端

| 指标 | 目标 |
|---|---|
| 读接口 P99 | **< 200ms** |
| 写接口 P99 | **< 300ms** |
| 列表分页查询 P95 | **< 150ms** |
| 缓存命中率（字典/权限/配置） | **> 90%** |
| 慢 SQL 阈值 | > 200ms 记录，> 1s 告警 |
| 慢接口阈值 | > 500ms 记录 |
| 单实例读 QPS | 压测确定，目标 ≥ 3000 |
| 启动时间 | JVM < 5s / Native < 1s |
| 常规负载内存 | **< 1GB** |
| OTel 采样率 | 生产 10%（错误全采） |

---

## 2. CI 性能门禁（防止性能腐化）

> 性能优化是一次性的，**门禁才是可持续的**。

```yaml
前端:
  - Lighthouse CI            # LCP / CLS / TBT 阈值门禁
  - size-limit / bundlesize  # 产物体积门禁，超限即失败
  - vitest bench             # 表格渲染基准，对比上次基线（> 15% 退化则失败）
  - Playwright + web-vitals  # 真实浏览器指标

后端:
  - JMH 微基准               # 权限计算、字典翻译等关键算法
  - k6 / Gatling 冒烟压测    # 每夜跑，P95 对比基线，退化则告警
  - 慢 SQL 扫描             # 全表扫描检测、缺失索引检测
```

**新增依赖必须评估产物体积影响**；超出预算需在 PR 中说明理由与替代方案。

---

## 3. 前端性能约束

### 3.1 分包（Vite 8 / Rolldown）

```ts
build: {
  rollupOptions: {
    output: {
      advancedChunks: {
        groups: [
          { name: 'vue',      test: /node_modules\/(vue|vue-router|pinia)\// },
          { name: 'naive',    test: /node_modules\/(naive-ui|css-render|@css-render)\// },
          { name: 'vxe',      test: /node_modules\/(vxe-table|xe-utils)\// },
          { name: 'echarts',  test: /node_modules\/(echarts|zrender)\// },
          { name: 'editor',   test: /node_modules\/(@tiptap|prosemirror)\// },
        ],
      },
    },
  },
}
```

### 3.2 首屏策略

- **不做 SSR**（中台鉴权复杂、SEO 无需求，SSR 收益低而成本高）
- 应用壳 + 登录页预渲染；业务页 SPA + **骨架屏**（首屏 HTML 内联骨架与关键 CSS）
- 资源提示：`<link rel="modulepreload">` + `preconnect`（API / OSS 域名）
- **路由预取**：鼠标 hover 菜单时预取对应 chunk
- 压缩：brotli（优先）+ gzip
- 图片：懒加载 + WebP/AVIF + **声明尺寸**（防 CLS）
- 字体：系统字体栈优先，避免 FOIT

### 3.3 ⚠️ Naive UI 的 CSS-in-JS 开销

Naive UI 基于 `css-render` 运行时注入样式。风险不在首屏 FOUC，而在：

- 大量组件实例（如 1000 行表格每格放 `n-tag`）→ 样式收集与 DOM 插入开销
- 频繁更新的列表 → 样式重算

**强制约束**：

| 规则 | 说明 |
|---|---|
| **表格热路径禁止放 Naive 组件** | 单元格用轻量 `span` + 类名，或 vxe-table 自渲染 |
| 虚拟滚动单元格内禁止组件库 | 用纯 DOM + CSS |
| 生产启用静态样式提取 | 按设计文档 §12.3 的降级预案 |
| **基准测试守护** | 1000 行 × 10 列含组件单元格的渲染耗时进 CI bench |

> 表格改用 vxe-table 自渲染是**结构性规避**，这是 ProTable 选 vxe 的隐藏收益。

---

## 4. 大表格极限场景

| 场景 | 方案 |
|---|---|
| 10 万行浏览 | vxe-table 虚拟滚动 + **服务端分页**（不要一次拉 10 万行） |
| 万行编辑 | 虚拟滚动 + 编辑模式 + 批量提交 |
| 列数 > 30 | 列设置 + 横向虚拟滚动 + 固定列 |
| 单元格含组件 | **自绘 span 替代组件库** |
| 导出 10 万行 | **后端流式导出**（EasyExcel），前端不参与 |
| 树形大数据 | 懒加载 + 服务端树查询 |
| 客户端排序/筛选 | 仅小数据量；大数据走服务端 |

**深分页**：用游标分页（`WHERE id > ? ORDER BY id LIMIT ?`），禁止大 `OFFSET`。

---

## 5. 接口瀑布治理

| 级别 | 方案 | 适用 |
|---|---|---|
| **L1（默认）** | 前端**并行请求**（`Promise.all` / TanStack Query `useQueries`） | 默认手段 |
| L2 | **后端批量接口**（`POST /batch`） | 首页 / 工作台 |
| L3 | **BFF 聚合** | 多端复用同一聚合时（谨慎引入） |

**默认只做 L1**，L3 仅在确有需要时引入（BFF 增加部署与维护成本）。

---

## 6. 后端与数据库性能

| 项 | 约束 |
|---|---|
| **连接池** | HikariCP `maximum-pool-size` **必须显式限制**；虚拟线程下用 `Semaphore` 保护 DB ⚠️ |
| **虚拟线程** | 禁止用 `synchronized` 包裹阻塞调用（pin 载体线程）→ 用 `ReentrantLock`；线程池包装注意上下文传递 |
| **CPU 密集任务** | 仍用平台线程池（虚拟线程只解决 I/O 阻塞） |
| 查询 | 禁止 `SELECT *`；禁止循环查询（N+1）；批量操作分批 ≤ 500 |
| 分页 | 分页必填；深分页用游标 |
| 缓存 | 多级（Caffeine 秒级 + Redis 分钟级）；防击穿（逻辑过期 + 互斥重建）；防雪崩（TTL 随机抖动） |
| 缓存监控 | 命中率暴露到 Prometheus，**低于阈值告警**（失效风暴是最常见线上事故） |
| 缓存原则 | **缓存不参与正确性，只参与性能** |
| 事务 | 短事务；禁止在事务内调用外部 HTTP / 发消息 |
| 大字段 | 列表查询不返回 `TEXT` / `JSON` 大字段 |
| 归档 | 日志按月分区；N 个月前归档 |
| 读写分离 | 读走从库；**写后立即读走主库** |

---

## 7. 性能红线

1. ❌ 列表接口返回大字段（`TEXT` / `JSON` / Base64）
2. ❌ 无 `LIMIT` 的全表查询进生产
3. ❌ 循环内查询（N+1）
4. ❌ 前端循环内发请求（改为并行或批量接口）
5. ❌ 表格单元格内放组件库组件
6. ❌ 不限制 HikariCP 连接池大小就用虚拟线程
7. ❌ `synchronized` 包裹阻塞调用（虚拟线程 pinning）
8. ❌ 引入新依赖不评估产物体积
9. ❌ 新增接口不关注 P99 / 慢 SQL
10. ❌ 在空表上验证查询性能就上线
