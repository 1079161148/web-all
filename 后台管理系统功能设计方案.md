# 企业级多租户中台系统 · 功能与架构设计方案

> **本文档是全项目唯一权威设计文档。** 所有开发、评审、规则配置均以本文档为准。

| 项 | 内容 |
|---|---|
| **文档版本** | v4.0（权威版，合并 v1 功能清单 + v2 架构设计 + v3 组件与体验设计） |
| **定位** | 面向多租户 SaaS 场景的企业级中台基座（DDD 内核 + 契约优先 + 可插拔业务插件） |
| **目标** | 在 **多租户、契约优先、Pro 组件层、可观测、测试体系** 五个维度超越 RuoYi / RuoYi-Vue-Plus / vue-pure-admin |
| **技术基线** | JDK 25 LTS · Spring Boot 4.0 · Vue 3.5 · Vite 8 · Naive UI 2.44+ · vxe-table 4.x · form-create v3 |
| **更新日期** | 2026-09 |

---

## 目录

- [一、项目定位与设计目标](#一项目定位与设计目标)
- [二、技术选型总表](#二技术选型总表)
- [三、总体架构](#三总体架构)
- [四、后端 DDD 架构](#四后端-ddd-架构)
- [五、功能模块清单](#五功能模块清单)
- [六、多租户设计](#六多租户设计)
- [七、四维权限模型](#七四维权限模型)
- [八、认证与授权（OAuth2.1 / OIDC）](#八认证与授权oauth21--oidc)
- [九、数据层设计](#九数据层设计)
- [十、契约优先流水线](#十契约优先流水线)
- [十一、前端架构](#十一前端架构)
- [十二、性能工程](#十二性能工程)
- [十三、可观测性与测试体系](#十三可观测性与测试体系)
- [十四、安全设计](#十四安全设计)
- [十五、平台治理](#十五平台治理)
- [十六、部署与 DevOps](#十六部署与-devops)
- [十七、分期落地路线图](#十七分期落地路线图)
- [十八、竞品差异化对照](#十八竞品差异化对照)
- [十九、架构决策记录（ADR）](#十九架构决策记录adr)
- [二十、坑位清单](#二十坑位清单)
- [二十一、P0 验证清单](#二十一p0-验证清单)

---

## 一、项目定位与设计目标

### 1.1 定位

**面向多租户 SaaS 场景的企业级中台基座**：DDD 内核 + 契约优先 + 可插拔业务插件。

这不是一次性交付的项目脚手架，而是**要长期演进的平台**。因此所有设计以"三年后仍可维护"为约束。

### 1.2 三条设计法则（宪法）

#### 法则一：二次封装 = 封装「约定与集成」，不封装「实现」

```
✅ 正确：ProTable —— 内核能力（虚拟滚动/行列编辑/树形/导出）100% 来自 vxe-table，
         我们只封装搜索区/工具栏/列设置/字典翻译/权限/分页契约/状态矩阵/主题桥接
❌ 错误：自己实现虚拟滚动、自己写编辑逻辑
```

**判定标准：如果封装层里有「算法」，就说明封错了。**

#### 法则二：UI 库只负责「基础元素」，重能力全部下沉到专业库

| 层 | 由谁负责 | 例子 |
|---|---|---|
| **基础元素** | Naive UI | 按钮、输入框、下拉、弹窗、抽屉、提示、标签、栅格 |
| **重能力组件** | **专业社区库** | 复杂表格 → vxe-table；动态表单 → form-create；上传 → Uppy；富文本 → Tiptap |
| **业务抽象** | `@admin/ui` | 只做组合、约定、DX、主题桥接 |

**结构性收益**：UI 库的生态风险被消除 —— 复杂能力本来就不该由 UI 库提供。

#### 法则三：先问「社区有没有」，有则封装，无则评估是否真的需要

1. 社区有成熟方案 → **封装，禁止自研**
2. 多个方案 → 选**维护活跃度 + 生态契合度**最高的（不是功能最多的）
3. 都没有 → **先问能不能不做**，最后才考虑自研

### 1.3 复杂度分级（团队认知成本控制）

> 同时引入 DDD + Monorepo + 多租户 + 契约优先 + 组件封装层，是 5 条陡峭学习曲线。**解法不是降低架构水平，而是提供三条进入通道。**

| 模式 | 适用场景 | 技术栈 | 上手成本 |
|---|---|---|---|
| **L1 简单模式** | 支撑域 CRUD（字典、公告、配置） | 代码生成器 + 事务脚本 + Pro 组件 | **30 分钟** |
| **L2 标准模式** | 常规业务模块 | 契约优先 + Pro 组件 + 页面模板 | **1 天** |
| **L3 严格模式** | 核心域（租户/权限/计费/流程） | DDD 聚合 + 领域事件 + 领域服务 | 需架构组 review |

ArchUnit 规则**按模式分层校验**（L1/L2 模块不强制 DDD 分层），避免规则被"注释掉"。

---

## 二、技术选型总表

> 原则：**每个位置只有一个答案**，并列选项等于没做决策。

### 2.1 后端

| 类别 | 选型 | 为什么是它 | 为什么不是另一个 |
|---|---|---|---|
| 运行时 | **JDK 25 LTS**（2025-09 GA） | 虚拟线程已修复 pinning、ScopedValue 正式、结构化并发、分代 ZGC | JDK 21 虚拟线程有 pinning 坑；JDK 17 拿不到并发红利 |
| 框架 | **Spring Boot 4.0**（Spring Framework 7） | 最低 JDK 17、一等支持 JDK 25，与 Security 7 / Modulith 2.x 对齐 | Boot 3 已进入维护期 |
| 模块化 | **Spring Modulith 2.x** | 单体内部强制模块边界 + 事务性事件 + 自动生成架构图 | 直接微服务：中台早期过度拆分，运维成本 > 收益 |
| 架构范式 | **DDD 四层 + CQRS 读写分离** | 核心域需要不变量保护与状态机 | 纯贫血 + 事务脚本：核心规则会散落到各处 |
| 持久化 | **MyBatis-Plus + 手写 XML** | 中后台 90% 是列表查询，对复杂动态 SQL 控制力最强。✅ **已验证**：官方 Boot 4 starter 存在（`com.baomidou:mybatis-plus-spring-boot4-starter:3.5.17`） | JPA：列表查询 N+1 与复杂分页难控。**不提供"或 JPA"选项** |
| 数据库迁移 | **Flyway** | 纯 SQL、可读、可 review、与 CI 天然契合 | Liquibase 可读性差；手工 SQL 不可审计 |
| 认证授权 | **Spring Authorization Server**（OAuth2.1 / OIDC） | PKCE、令牌吊销、SSO 联邦、MFA，标准协议不被推翻 | 自研 JWT：踢人下线/多端/SSO 全靠补丁 |
| 缓存 | **Redis 8.x** + Redisson（仅分布式锁） | 令牌、权限缓存、字典、租户配置、限流。✅ 本地已实测：`redis:8-alpine`（8.10.1）容器，健康检查 `redis: UP` | 本地 Caffeine 多实例不一致（两者组合见 §9.5） |
| 文档 | **springdoc-openapi 3.x** | 是**契约源**，不只是文档页（见 §十）。✅ 已核实最新稳定版 `3.1.1` | Knife4j 仅作 UI 增强层 |
| 领域事件 | **Spring 事件 + Modulith 事务性 Outbox** | 保证"业务落库成功才发事件" | 裸 MQ：双写不一致 |
| 消息队列 | **Kafka / RocketMQ**（P4 引入，可选） | 仅用于跨系统集成事件 | 单体阶段不需要，避免过早引入 |
| 对象存储 | **S3 协议抽象**（MinIO / OSS / COS） | 一套接口多实现，本地开发用 MinIO | 直接绑 OSS SDK 会被云厂商锁定 |
| 任务调度 | **PowerJob / XXL-Job** | 需要分片、失败重试、可视化、多租户隔离 | `@Scheduled` 多实例重复执行 |
| 映射 | **MapStruct** | 编译期生成，无反射开销 | BeanUtils 反射拷贝：性能差且静默丢字段 |
| Excel | **EasyExcel / FastExcel** | 流式读写，百万行不 OOM | POI 原生内存爆炸 |
| 可观测 | **Micrometer + OpenTelemetry + OTLP** | 厂商中立，可接 SkyWalking / Jaeger / Tempo | 直接绑 Agent 有字节码兼容风险 |
| 测试 | **JUnit 5 + AssertJ + Testcontainers + ArchUnit** | 真实依赖集成测试 + 架构约束可执行化 | H2 与 MySQL 行为不一致，测了个假 |
| 质量 | **Spotless + Checkstyle + PMD + SpotBugs** | 格式与缺陷前置到构建期 | 只靠 review 不可持续 |
| 构建 | **Maven 多模块**（或 Gradle JVM 8.x） | 依赖方向由模块图天然约束 | 单模块多包：依赖方向无人能阻止 |

### 2.2 前端

| 类别 | 选型 | 为什么是它 | 为什么不是另一个 |
|---|---|---|---|
| 框架 | **Vue 3.5.x**（3.6 / Vapor 观察后渐进启用） | 3.6 Vapor Mode 尚处 RC 末期，UI 库未适配 | 直接上 Vapor：Naive UI 不兼容 |
| 构建 | **Vite 8**（Rolldown 统一引擎） | dev 与 build 同一引擎，消除"本地正常线上白屏"；构建提速数倍 | Vite 6/7 双引擎行为不一致 |
| UI 库 | **Naive UI 2.44+** | themeOverrides 全量 TS 类型 + 暗色开箱即用 + 无 CSS 文件引入 | Element Plus 主题定制类型弱；**通过 `packages/ui` 隔离，不锁死** |
| 语言 | **TypeScript 5.x**（`strict: true`） | 契约优先的收益全靠类型系统兑现 | `strict: false` 让契约生成的类型形同虚设 |
| 包管理 | **pnpm 10 + workspace** | 硬链接省磁盘、规避依赖幽灵 | npm/yarn monorepo 体验差 |
| 任务编排 | **Turborepo** | 增量构建缓存，CI 提速 | 手工脚本无缓存无依赖图 |
| 状态（客户端） | **Pinia** | 主题、布局、标签页、用户会话 | Pinia 存服务端数据：缓存失效逻辑易错 |
| 状态（服务端） | **TanStack Query** | 缓存、失效、重试、乐观更新、请求去重 | 手写 loading/error：每页重复 50 行 |
| 路由 | **Vue Router 4** | 静态白名单 + 后端动态注册 | — |
| 请求 | **ofetch / ky**（契约生成客户端内聚封装） | 原生 fetch、体积小、AbortController 友好 | Axios 在契约优先模式下价值下降 |
| 样式 | **UnoCSS** + **CSS Variables**（Design Token） | 与 Naive themeOverrides 共用一套 Token | Tailwind 与 Naive CSS-in-JS 变量体系需额外桥接 |
| 表单校验 | **zod**（由 OpenAPI 生成） + form-create | 前后端校验规则同源 | 手写 rules：无法复用且易与后端不一致 |
| 复杂表格 | **vxe-table 4.x** | 中后台表格事实标准，虚拟滚动/编辑/树形/导出齐全 | n-data-table 复杂场景弱；ag-grid 体积与生态不匹配 |
| 富文本 | **Tiptap 3**（深度定制）或 **wangEditor 5**（开箱快） | 无头、可扩展；或中文友好省事 | — |
| 图表 | **ECharts 6 + vue-echarts** | 大屏与工作台事实标准 | — |
| 图标 | **@vicons**（xicons） | Naive UI 官方配套 | 混用 iconfont 有体积与一致性问题 |
| 国际化 | **vue-i18n 11** | 菜单/按钮/校验消息全量可切 | Naive UI 无内置 i18n，需自行包 |
| 代码质量 | **Oxlint**（替代 ESLint） + Stylelint + Prettier | Vite 8 生态默认推荐，快数十倍 | ESLint 在 Monorepo 大项目下分钟级 |
| 提交规范 | **husky + lint-staged + commitlint** | 提交即守门 | — |
| 单元测试 | **Vitest 4 + Vue Test Utils** | 与 Vite 同配置 | Jest 需额外转译链 |
| E2E | **Playwright** | 跨浏览器、自动等待、trace 回放 | Cypress 与 Vite 契合度更低 |
| 组件工作台 | **Histoire**（备选 Storybook） | Vue 生态原生 | — |
| 文档站 | **VitePress** | 轻量、Markdown 驱动 | — |
| Mock | **MSW** + OpenAPI 自动生成 handler | 与契约流水线天然集成 | — |
| 错误监控 | **Sentry** | 与后端 TraceID 打通 | 无监控靠用户截图排障 |

---

## 三、总体架构

### 3.1 分层视图

```
┌─────────────────────────────────────────────────────────────────────┐
│  接入层  Nginx / 网关（TLS 终止、限流、静态资源、/api 反代）          │
└─────────────────────────────────────────────────────────────────────┘
                                  │
┌─────────────────────────────────────────────────────────────────────┐
│  前端  pnpm Monorepo                                                │
│  apps/admin（主应用） · packages/ui（Pro 组件库）                    │
│  packages/api（OpenAPI 生成） · packages/theme（Design Token）       │
└─────────────────────────────────────────────────────────────────────┘
                                  │  HTTPS / OIDC / JSON
┌─────────────────────────────────────────────────────────────────────┐
│  后端  模块化单体（Spring Modulith）                                 │
│                                                                     │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐              │
│  │ IAM 上下文     │ │ Organization  │ │ Platform 上下文│              │
│  │ (核心域)       │ │ (支撑域)       │ │ (支撑域)       │              │
│  │ 租户/用户/角色 │ │ 部门/岗位      │ │ 字典/配置/文件 │              │
│  │ 权限/数据权限  │ │               │ │ 公告/消息/任务 │              │
│  └───────────────┘ └───────────────┘ └───────────────┘              │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐              │
│  │ Audit 上下文   │ │ Tooling 上下文 │ │ Workflow(可选) │              │
│  │ 登录/操作日志  │ │ 代码生成器     │ │ 流程引擎       │              │
│  └───────────────┘ └───────────────┘ └───────────────┘              │
│                                                                     │
│  横切：认证鉴权 · 多租户 · 审计 · 幂等 · 限流 · 缓存 · 可观测         │
└─────────────────────────────────────────────────────────────────────┘
                                  │
┌─────────────────────────────────────────────────────────────────────┐
│  MySQL 8.x（主从，逻辑删除 + 乐观锁 + tenant_id）                     │
│  Redis 8.x（令牌/权限缓存/租户配置/限流）                             │
│  对象存储（S3 协议） · 消息队列（P4） · 可观测后端（OTLP）             │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 架构原则（不可违反）

1. **依赖倒置**：领域层不依赖任何框架。仓储接口在领域层定义，实现在基础设施层。
2. **契约单一来源**：前后端接口定义只有后端 OpenAPI 一处，前端类型全部生成，禁止手写。
3. **租户上下文全链路**：任何数据访问、缓存 Key、文件路径、异步任务都必须携带 `tenantId`。
4. **权限后端兜底**：前端权限控制仅影响体验，后端必须独立鉴权。
5. **模块边界由 CI 强制**：ArchUnit + Spring Modulith 校验，破坏即构建失败。
6. **服务端状态不进 Pinia**：服务端数据归 TanStack Query。

---

## 四、后端 DDD 架构

### 4.1 务实 DDD：三级建模策略

> **最重要的免责声明**：对 CRUD 型模块全量套用 DDD（聚合 + 领域事件 + 仓储）会让开发效率下降 30~50%，团队认知成本极高。
> **判据：如果一个模块的业务规则能用一句 `if` 表达完，它就不配 DDD。**

| 级别 | 适用模块 | 建模方式 | 示例 |
|---|---|---|---|
| **A 富领域模型** | 核心域：有业务不变量、状态机、复杂规则 | 聚合根 + 值对象 + 领域事件 + 仓储 + 领域服务 | 租户生命周期、套餐配额、用户状态机、角色授权策略、工作流 |
| **B 事务脚本 + 仓储** | 支撑域：以 CRUD 为主，规则少 | Entity + Repository + AppService（**不搞聚合，不搞领域事件**） | 字典、参数配置、公告、岗位、文件记录 |
| **C 纯读写** | 日志、统计、报表 | QueryService + Mapper 直接返回 VO，**完全绕过领域层** | 操作日志、登录日志、监控数据 |

### 4.2 限界上下文划分

| 上下文 | 类型 | 职责 | 表前缀 |
|---|---|---|---|
| **IAM** | 核心域 | 租户、用户、角色、菜单权限、数据权限、字段权限 | `iam_` |
| **Organization** | 支撑域 | 部门树、岗位、用户归属 | `org_` |
| **Platform** | 支撑域 | 字典、参数配置、通知公告、文件、站内消息 | `plt_` |
| **Audit** | 支撑域 | 登录日志、操作日志、在线用户 | `aud_` |
| **Tooling** | 支撑域 | 代码生成器、元数据管理 | `tls_` |
| **Workflow** | 核心域（可选） | 流程定义、流程实例、审批任务 | `wf_` |

**跨上下文通信规则**：

- 同进程内 → Spring Modulith 的 `@ApplicationModuleListener`（事务提交后触发）
- **禁止直接跨上下文引用对方 Repository / Entity**
- 需要跨上下文数据 → 通过对方的 **公开 API（Named Interface）** 或**集成事件**

### 4.3 四层分层与依赖方向

```
        ┌─────────────────────────────────┐
        │  interfaces（用户接口层）        │  Controller / Assembler / 参数校验
        │  —— 依赖 application             │  REST + OpenAPI 注解
        └─────────────────────────────────┘
                       ↓
        ┌─────────────────────────────────┐
        │  application（应用层）           │  AppService / Command / Query
        │  —— 依赖 domain，事务边界在此     │  DTO / Assembler / 事件发布
        └─────────────────────────────────┘
                       ↓
        ┌─────────────────────────────────┐
        │  domain（领域层）★ 零框架依赖    │  聚合根 / 实体 / 值对象
        │  —— 不依赖任何其他层              │  领域事件 / 仓储接口 / 领域服务
        └─────────────────────────────────┘
                       ↑ 依赖倒置（实现 domain 定义的接口）
        ┌─────────────────────────────────┐
        │  infrastructure（基础设施层）    │  RepositoryImpl / MyBatis Mapper
        │  —— 依赖 domain                  │  PO / 缓存 / 外部防腐层(ACL)
        └─────────────────────────────────┘
```

**硬性约束（ArchUnit 校验，破坏即构建失败）**：

| 层 | 禁止依赖 |
|---|---|
| `domain` | `org.springframework.*`、`com.baomidou.*`、`jakarta.persistence.*`，以及 `application` / `infrastructure` / `interfaces` |
| `application` | `infrastructure`（只能依赖 domain 定义的接口） |
| `interfaces` | 直接依赖 `infrastructure` |

**端口与适配器**：领域层需要外部能力（密码编码、缓存、外部 API）时，在 `domain` 定义 **Port 接口**，在 `infrastructure` 实现。

```java
// ✅ domain 层定义
public interface PasswordEncoderPort {
    EncodedPassword encode(RawPassword raw);
}

// ✅ infrastructure 层实现（包装 Spring Security）
@Repository
public class PasswordEncoderAdapter implements PasswordEncoderPort { ... }
```

### 4.4 Maven 多模块工程结构

```
server/
├─ admin-parent/                      # 聚合 POM，统一依赖版本
├─ admin-common/                      # 通用能力：Result/PageResult/异常/常量/工具
│                                     #  含 TenantContext / UserContext / TraceContext
├─ admin-domain/                      # ★ 领域层（零框架依赖）
│   └─ .../domain/
│       ├─ iam/                       #   tenant/ user/ role/ permission/ datascope/
│       │   ├─ model/                 #     聚合根、实体、值对象
│       │   ├─ event/                 #     领域事件
│       │   ├─ repository/            #     仓储接口（仅接口！）
│       │   └─ service/               #     领域服务（跨聚合不变量）
│       ├─ organization/
│       ├─ platform/
│       └─ shared/                    #  共享内核：TenantId、UserId、Money 等值对象
├─ admin-application/                 # 应用层
│   └─ .../application/
│       ├─ iam/                       #  AppService、Command、Query、DTO、Assembler
│       └─ ...
├─ admin-infrastructure/              # 基础设施层
│   └─ .../infrastructure/
│       ├─ persistence/               #  PO、Mapper、RepositoryImpl、Converter
│       ├─ cache/                     #  Redis 实现
│       ├─ storage/                   #  文件存储（S3 抽象实现）
│       ├─ acl/                       #  防腐层：第三方系统适配
│       └─ config/                    #  MyBatis / Redis / OTel 等配置
├─ admin-interfaces/                  # 用户接口层
│   └─ .../interfaces/
│       ├─ rest/                      #  Controller、Request/Response、Assembler
│       └─ job/                       #  定时任务入口
├─ admin-bootstrap/                   # 启动模块（唯一有 main 的模块）
│   └─ src/main/resources/
│       ├─ application.yml
│       └─ db/migration/              #  Flyway 脚本
└─ admin-test-support/                # 测试基类、Testcontainers 单例容器、测试数据构造器
```

**可选进阶**：当某上下文需独立演进时，可拆为 `admin-iam-domain` / `admin-iam-application`。**但不要提前拆**，过早拆分带来大量 POM 维护成本。

### 4.5 核心域建模示例（IAM）

#### 租户聚合（含状态机与配额）

```java
// domain/iam/model/tenant/Tenant.java —— 零框架注解
public class Tenant {
    private final TenantId id;
    private TenantCode code;
    private TenantName name;
    private TenantStatus status;       // 值对象，内部持有状态机
    private SubscriptionPlan plan;     // 值对象
    private Quota quota;               // 值对象：用户数/存储/API 调用配额
    private ExpireTime expireTime;
    private final List<DomainEvent> events = new ArrayList<>();

    /** 激活：仅允许从 PENDING 流转 */
    public void activate(Clock clock) {
        if (!status.canTransitTo(TenantStatus.ACTIVE)) {
            throw new IllegalTenantStateException(status, TenantStatus.ACTIVE);
        }
        this.status = TenantStatus.ACTIVE;
        this.events.add(new TenantActivatedEvent(id, clock.instant()));
    }

    /** 续期：延长到期时间，同时重置配额周期 */
    public void renew(Period duration, SubscriptionPlan newPlan, Clock clock) {
        this.expireTime = expireTime.plus(duration);
        this.plan = newPlan;
        this.quota = newPlan.initialQuota();
        this.events.add(new TenantRenewedEvent(id, newPlan.code(), clock.instant()));
    }

    /** 配额扣减：不变量——不可为负 */
    public void consumeQuota(QuotaType type, long amount) {
        this.quota = quota.consume(type, amount);   // Quota 内部校验并抛异常
    }

    /** 是否可登录：过期 / 挂起 / 关闭均拒绝 */
    public boolean isAccessible(Instant now) {
        return status == TenantStatus.ACTIVE && expireTime.isAfter(now);
    }
}
```

**要点**：
- 状态流转合法性由**值对象 `TenantStatus`** 内部的状态机保证，而非散落在 Service 的 `if`
- 每个业务动作产出**领域事件**，用于缓存失效 / 审计 / 通知
- `Clock` 作为参数注入，保证可测试（不用 `Instant.now()`）

#### 用户聚合

```java
public class User {
    private final UserId id;
    private final TenantId tenantId;      // 租户归属不可变
    private Username username;
    private EncodedPassword password;     // 值对象，内部保证 hash 算法一致
    private Email email;
    private PhoneNumber phone;
    private UserStatus status;            // ACTIVE / LOCKED / DISABLED
    private DeptId deptId;
    private final Set<RoleId> roleIds;    // 只持有 ID 引用，不持有 Role 实体

    /** 改密：编码由外部策略注入，领域内不依赖 Spring Security */
    public void changePassword(RawPassword raw, PasswordEncoderPort encoder) {
        this.password = encoder.encode(raw);
        this.status = status.unlockIfLocked();  // 改密顺带解锁
        this.events.add(new UserPasswordChangedEvent(id, tenantId));
    }

    public void lock(String reason) { ... }

    public void assignRoles(Set<RoleId> newRoleIds, RoleAssignPolicy policy) {
        policy.validate(newRoleIds, this);   // 策略对象承载授权规则
        this.roleIds.clear();
        this.roleIds.addAll(newRoleIds);
        this.events.add(new UserRolesChangedEvent(id, tenantId, newRoleIds));
    }
}
```

**关键设计**：
- `PasswordEncoderPort` 是**领域层定义的端口接口**，实现在 infrastructure → 领域层零框架依赖
- 用户-角色是 **ID 引用**，不是对象引用 → 避免跨聚合一致性负担
- `roleIds` 变更产出事件 → 触发权限缓存失效

#### 值对象清单

| 值对象 | 不变量 |
|---|---|
| `TenantId` / `UserId` / `RoleId` | 非空、正数 |
| `TenantCode` | 6-32 位小写字母数字连字符、全局唯一、创建后不可改 |
| `Username` | 4-32 位、字母开头、唯一（**租户内**） |
| `RawPassword` / `EncodedPassword` | 强度策略 / 算法标识 |
| `Email` / `PhoneNumber` | 格式校验 |
| `DataScope` | 枚举 + 自定义部门集合 |
| `PermissionCode` | `{context}:{resource}:{action}` 三段式 |
| `Quota` | 各维度非负 |
| `Money` | BigDecimal + 币种 |

#### 聚合建模规则

- **零框架注解** —— 聚合根上不得出现 `@TableName` / `@Entity` / `@Service`
- **不变量由聚合保证**，不散落到 AppService
- **业务动作产出领域事件**，不在 Service 里"顺手清缓存"
- **时间通过 `Clock` 注入**，禁止在领域层直接调 `Instant.now()`
- **跨聚合只持 ID 引用**，不持对象引用
- **一次事务只修改一个聚合**；跨聚合一致性由领域服务编排或最终一致解决
- **聚合根要小** —— 把角色、部门塞进 User 聚合是典型错误

### 4.6 应用层设计

```java
@Service
public class TenantAppService {
    private final TenantRepository tenantRepository;
    private final ApplicationEventPublisher eventPublisher;

    /** 命令：应用层是唯一的事务边界 */
    @Transactional(rollbackFor = Exception.class)
    public TenantId createTenant(CreateTenantCommand cmd) {
        // 1. 幂等校验（Redis SETNX，见 §14.3）
        // 2. 唯一性校验（跨聚合规则 → 用仓储查询）
        if (tenantRepository.existsByCode(cmd.code())) {
            throw new BizException(ErrorCode.TENANT_CODE_DUPLICATED);
        }
        // 3. 构造聚合
        Tenant tenant = Tenant.create(cmd.code(), cmd.name(), cmd.plan(), clock);
        // 4. 持久化
        tenantRepository.save(tenant);
        // 5. 发布领域事件（Modulith 保证落库后投递）
        tenant.pullEvents().forEach(eventPublisher::publishEvent);
        return tenant.id();
    }

    /** 查询：直接走 QueryService，不做领域转换，性能优先（CQRS 读模型） */
    public PageResult<TenantVO> pageTenant(TenantQuery query) {
        return tenantQueryService.page(query);
    }
}
```

**约定**：

1. **事务边界只在 AppService** —— Controller 与 Repository 都不得开事务
2. **AppService 只做编排**：加载聚合 → 调领域方法 → 保存 → 发事件。**禁止在 AppService 写业务分支 `if`**
3. `Command` / `Query` 用 `record`（不可变）
4. **CQRS 读写分离**：写走聚合 + Repository；读走 QueryService 直查 VO，零转换开销

### 4.7 领域事件与事务性 Outbox

```java
@Component
class TenantEventHandlers {
    @ApplicationModuleListener                 // = AFTER_COMMIT + @Async
    void on(TenantSuspendedEvent event) {
        permissionCache.evictByTenant(event.tenantId());
        onlineUserRegistry.kickAllOf(event.tenantId());
        auditRecorder.record(event);
    }
}
```

**解决的核心问题**：裸 `@EventListener` 在事务提交前投递 —— 事务一旦回滚，缓存已清、消息已发，数据不一致。
**Modulith 的 Outbox**：事件先写入 `event_publication` 表（与业务同事务），提交后由后台任务投递并标记完成，保证 **at-least-once**。事件处理器必须幂等。

### 4.8 仓储与持久化：三层对象分离（DDD + MyBatis 的核心难点）

> MyBatis-Plus 的 `@TableName` 实体是贫血的、带框架注解的，**与充血聚合根天然冲突**。

| 层 | 对象 | 特征 |
|---|---|---|
| `domain` | **聚合根 / 实体** | 充血、零框架注解、含业务方法 |
| `infrastructure` | **PO**（`TenantPO`） | 贫血、`@TableName` / `@TableId`、对应表结构 |
| `application` | **DTO / VO** | 对外契约，不含业务方法 |

```java
// infrastructure/persistence/iam/TenantRepositoryImpl.java
@Repository
@RequiredArgsConstructor
public class TenantRepositoryImpl implements TenantRepository {   // ★ 实现 domain 的接口
    private final TenantMapper mapper;
    private final TenantConverter converter;           // MapStruct

    @Override
    public Optional<Tenant> findById(TenantId id) {
        return Optional.ofNullable(mapper.selectById(id.value()))
                       .map(converter::toDomain);
    }

    @Override
    public void save(Tenant tenant) {
        TenantPO po = converter.toPO(tenant);
        if (mapper.selectById(po.getId()) == null) {
            mapper.insert(po);
        } else {
            // 乐观锁：version 不匹配则抛异常而非静默覆盖
            if (mapper.updateById(po) == 0) {
                throw new ConcurrentModificationException("租户并发修改冲突: " + po.getId());
            }
        }
    }
}
```

**性能权衡**：
- **核心域（写路径）**：接受转换成本，换取模型纯净
- **支撑域 / 读路径**：QueryService 直接 `SELECT` 到 VO，**完全绕过领域层**，零转换开销

**仓储接口规则**：定义在 `domain`，实现在 `infrastructure`；命名与语义用领域语言（`findById` / `save` / `existsByCode`），不暴露 SQL 概念。

### 4.9 代码生成器与 DDD 的关系

代码生成器（选表 → 生成全栈代码）与 DDD 存在张力：**生成的贫血 CRUD 会污染核心域**。

**策略**：

| 目标模块 | 生成内容 |
|---|---|
| **支撑域**（字典/配置/公告等） | 生成完整四层样板：PO + Mapper + Repository + AppService + DTO + Controller + Flyway 脚本 |
| **核心域** | 只生成**测试脚手架 + 仓储接口骨架 + 领域事件骨架**，领域模型必须手写 |
| **前端** | 生成 **ProTable / ProForm 的 schema 配置 JSON**，而非写死的 `.vue` 页面 |

**升级方向**：从"生成代码"升级为"**元数据驱动运行时**" —— 表结构 → 元数据 → 前端 schema JSON → 通用渲染。这才是中台相对脚手架的质变。

### 4.10 横切关注点归属

| 关注点 | 实现位置 | 说明 |
|---|---|---|
| 事务 | `application`（AppService） | 唯一入口 |
| 审计日志 | `infrastructure` AOP + `@Audit` 注解 | 切 Controller，记录操作人/耗时/参数（脱敏后） |
| 多租户 | `infrastructure` MyBatis 拦截器 | 自动拼 `tenant_id`（见 §6.3） |
| 数据权限 | `infrastructure` MyBatis 拦截器 | 自动拼部门范围，**须在分页拦截器之前** |
| 字段权限 | `infrastructure` Jackson 序列化器 | 按角色脱敏（见 §7.4） |
| 幂等 | `infrastructure` `@Idempotent` + Redis SETNX | 只作用于写接口 |
| 限流 | `infrastructure` `@RateLimiter` + Redis 令牌桶 | 维度：IP / 用户 / 租户 |
| 缓存 | `domain` 定义 Port，`infrastructure` 实现 | 领域层只调 `PermissionCachePort` |
| 可观测 | `infrastructure` 自动配置 | OTel 自动埋点 + `@Observed` 自定义 Span |

---

## 五、功能模块清单

### 5.1 认证与账户

- 账号密码登录（BCrypt 加密 + 前端 RSA/SM2 传输加密）
- 图形验证码（Redis 存储，可开关）、登录失败 N 次锁定账户
- **OAuth2.1 / OIDC 标准令牌**：`accessToken`（JWT，15min）+ `refreshToken`（opaque，7d，**可精确吊销**）
- 单点登录 / 强制下线 / 异地登录踢出
- 多因子认证（TOTP）、敏感操作二次认证
- 记住我、退出登录
- 第三方联邦登录（企业微信 / 钉钉 / LDAP / AD）
- 个人中心：资料修改、头像裁剪上传、修改密码、绑定手机号/邮箱

### 5.2 系统管理

| 模块 | 功能点 |
|---|---|
| 租户管理 | 增删改查、套餐与配额、生命周期状态机（待激活/正常/停用/过期/关闭）、租户初始化、租户级配置覆盖 |
| 用户管理 | 增删改查、导入导出 Excel、重置密码、分配角色、启用/禁用、解锁、字段级脱敏 |
| 角色管理 | 分配菜单权限、分配数据范围、分配字段权限、角色用户列表 |
| 菜单管理 | 目录/菜单/按钮三级树、图标、排序、外链、缓存、隐藏、组件路径 |
| 部门管理 | 树形组织、上下级、负责人、状态 |
| 岗位管理 | 岗位编码、排序 |
| 字典管理 | 字典类型 + 字典数据，平台级默认 + 租户级覆盖，前端 `useDict` 自动拉取并缓存 |
| 参数配置 | 平台默认 + 租户覆盖两级 fallback，Redis 缓存 |
| 通知公告 | 富文本编辑、置顶、定时发布、已读未读 |

### 5.3 监控与运维

> 最容易被忽略、最能体现专业度的一环。

- **在线用户**：实时查看、强制下线
- **登录日志**：IP、地点、浏览器、操作系统、状态、耗时（按月分区）
- **操作日志**：`@Audit` 注解 + AOP 自动记录，含请求参数、返回值、异常信息、耗时（按月分区）
- **定时任务**：Cron 表达式、在线启停、执行一次、执行日志，支持租户级/平台级任务
- **服务监控**：CPU / 内存 / 磁盘 / JVM / 系统信息（Actuator + OSHI）
- **缓存监控**：Redis 命令统计、Key 数量、内存占用、缓存清理、**命中率曲线**
- **接口文档**：springdoc + Knife4j 在线调试（**生产禁用**）
- **全链路追踪**：按 traceId 查询请求链路（见 §13.1）
- **SQL 慢日志 / 接口慢响应 Top N**：性能排查利器
- **租户用量看板**：用户数、存储、API 调用量按租户计量与展示

### 5.4 基础设施能力

- **文件管理**：上传（单/多文件、分片、断点续传）、下载、预览、删除；统一文件表；S3 协议抽象多实现
- **消息中心**：站内信 + SSE 实时推送，多通道（站内信 / 邮件 / 短信 / 推送）
- **导入导出**：EasyExcel 模板导出 + 批量导入（含错误行回写）
- **代码生成器**：元数据驱动（见 §4.9）
- **接口限流**：Redis + 注解（IP / 用户 / 租户三维度）、防重复提交幂等注解
- **国际化**：中英文切换（菜单、按钮、校验提示），含字段级翻译与时区处理
- **Feature Flag**：全局 / 租户 / 用户 / 百分比维度，可灰度（见 §15.4）
- **插件机制**：后端 SPI + 前端约定式插件（见 §15.3）
- **多数据源**（可选）：主从 / 多租户隔离策略切换

### 5.5 产品体验与全局能力

> 这一层决定"好用易用"，是竞品胜负手所在。

| 能力 | 说明 |
|---|---|
| 多标签页（TagsView） | 标签栏 + 右键菜单 + 页面缓存 `<keep-alive>` |
| 命令面板（Cmd+K） | 全局搜菜单/页面/操作 |
| 全局搜索 | 后端统一搜索接口 + 前端面板 |
| 快捷键体系 | 高频操作效率 |
| 面包屑 / 全屏 / 主题换肤 | 暗黑模式 + 主题色 + 布局切换 |
| 工作台卡片个性化 | 拖拽布局 + 持久化 |
| 新手引导 | driver.js + 后端存储已读状态 |
| 状态矩阵 | 加载 / 空 / 错误 / 无权限 / 部分失败 五态统一（见 §11.9） |
| 响应式 | PC 完整 / 平板自适应 / 移动端独立入口（见 §11.12） |
| 无障碍 a11y | 键盘可达 + 焦点管理 + 对比度 + axe 自动化检测（见 §11.11） |

### 5.6 平台治理能力

文档体系、升级机制、插件 SPI、Feature Flag、协作规范、依赖 EOL 治理、团队认知分级 —— 详见 [§十五 平台治理](#十五平台治理)。

### 5.7 业务扩展模块（按行业选装）

- 电商：商品、订单、库存、售后、优惠券
- 内容：文章、分类、标签、评论
- 工作流：流程设计、审批流（Flowable + LogicFlow）
- 数据大屏：可视化图表看板
- 开放平台：API Key 管理、开发者门户、调用量统计
- AI 能力：自然语言生成 CRUD / 智能问数 / AI 助手

---

## 六、多租户设计

> **定位**：v1 把多租户放在"（可选）"，本方案提到 **P0 一等公民**。这是 RuoYi-Vue-Plus 打赢 RuoYi 的核心武器。

### 6.1 隔离策略对比

| 策略 | 隔离度 | 成本 | 适用 |
|---|---|---|---|
| **独立数据库** | 最高 | 最高（连接池 ×N） | 金融、政企强合规客户 |
| **独立 Schema** | 高 | 高（需动态切换 Schema） | 中大客户混合 |
| **共享表 + `tenant_id`** ★ | 中 | 低 | **默认策略** |

**架构目标**：三种策略**可插拔**，通过 `TenantIsolationStrategy` 接口切换，默认 `COLUMN`。

### 6.2 租户上下文

```java
public final class TenantContext {
    private static final ThreadLocal<TenantId> CURRENT = new ThreadLocal<>();

    public static void set(TenantId id) { CURRENT.set(id); }
    public static TenantId get() { return CURRENT.get(); }
    public static TenantId require() { ... }   // 缺失即抛异常，防漏隔离
    public static void clear() { CURRENT.remove(); }
}
```

**必须解决的三个穿透场景**（最容易出隔离事故）：

| 场景 | 问题 | 方案 |
|---|---|---|
| **异步线程池** | `ThreadLocal` 不跨线程 | **TransmittableThreadLocal（TTL）** 包装线程池；JDK 25 虚拟线程场景优先用 **`ScopedValue`** |
| **定时任务** | 无请求上下文 | 任务执行前遍历租户列表，逐个 `set / finally clear` |
| **MQ / 事件监听** | 消费端无上下文 | 消息体**强制携带 `tenantId`**，消费入口先 `set` |

### 6.3 MyBatis 租户拦截器

```java
@Bean
MybatisPlusInterceptor mybatisPlusInterceptor() {
    MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
    // ★ 顺序敏感，见 §7.3
    interceptor.addInnerInterceptor(new TenantLineInnerInterceptor(tenantLineHandler()));
    interceptor.addInnerInterceptor(new DataPermissionInterceptor(...));
    interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
    interceptor.addInnerInterceptor(new OptimisticLockerInnerInterceptor());
    return interceptor;
}
```

**忽略名单必须显式声明**（`@InterceptorIgnore(tenantLine = "true")` 或表名白名单）：

- 平台级表：`iam_tenant`、`plt_dict_type`（字典可能是全局共享）
- 系统表：`event_publication`、`flyway_schema_history`
- 跨租户统计场景（需业务方显式声明，**禁止默认放行**）

### 6.4 多租户完整影响面清单

| 影响面 | 处理方式 |
|---|---|
| 数据库 | `tenant_id` 列 + **所有唯一索引改为 `(tenant_id, xxx)` 联合唯一** ← 极易漏 |
| 缓存 | Redis Key 前缀 `{app}:{tenantId}:...`，**禁止全局 Key** |
| 文件存储 | 路径 `/tenant/{tenantId}/...`，防止越权下载 |
| 定时任务 | 租户级 / 平台级任务分离 |
| 字典 & 配置 | 平台级默认值 + 租户级覆盖两级 fallback |
| 日志 | MDC 注入 `tenantId`，便于按租户排障 |
| 消息通知 | 模板按租户隔离，避免串号 |
| 配额 | 用户数 / 存储 / API 调用量按租户计量与限流 |
| 账号唯一性 | 用户名在**租户内**唯一，而非全局 |
| 租户生命周期 | PENDING → ACTIVE → SUSPENDED / EXPIRED → CLOSED（由聚合状态机保证） |

---

## 七、四维权限模型

```
User ──多对多──> Role ──多对多──> Menu/Permission
                   │
                   ├── dataScope   : 全部 / 自定义 / 本部门 / 本部门及以下 / 仅本人
                   └── fieldPolicy : 字段可见性 / 脱敏规则
User ──> Dept（树形，ancestors 物化路径）
User ──> Tenant（租户隔离，最外层边界）
```

### 7.1 菜单 / 路由权限

- 登录后由后端返回**菜单树**，前端 `router.addRoute`
- `component` 字段存**字符串路径**，前端用 `import.meta.glob('@/views/**/*.vue')` 建映射表查表
- 前端只存映射表，**不硬编码任何菜单**
- 刷新恢复：菜单树缓存到 `sessionStorage`，路由守卫内 `addRoute` 后 `next({ ...to, replace: true })`

### 7.2 按钮 / 接口权限

| 侧 | 实现 |
|---|---|
| 前端 | `v-permission="'iam:user:add'"` 指令，`mounted` 时移除无权限节点（比 `v-if` 更难绕过） |
| 后端 | `@PreAuthorize("@ps.hasPermission('iam:user:add')")` + 自定义 `PermissionService` Bean |

**权限码格式**：`{context}:{resource}:{action}`，如 `iam:user:add`。

**后端权限判定链**：`用户 → 角色 → 权限集合（Redis 缓存，key: iam:perm:{tenantId}:{userId}）`，角色权限变更时按事件失效。

> **红线**：前端权限控制只影响体验，**后端必须独立鉴权**。任何新增写接口必须加权限注解。CI 应扫描 Controller 是否漏加。

### 7.3 数据权限

```java
@Target(ElementType.METHOD)
public @interface DataScope {
    String deptAlias() default "";
    String userAlias() default "";
}
```

MyBatis 拦截器在 SQL 执行前，按当前用户角色的 `dataScope` 拼接条件：

| dataScope | 拼接条件 |
|---|---|
| ALL | 无条件 |
| CUSTOM | `dept_id IN (角色自定义部门集)` |
| DEPT | `dept_id = 当前用户部门` |
| DEPT_AND_CHILD | `dept_id IN (本部门及子部门，基于 ancestors 前缀匹配)` |
| SELF | `create_by = 当前用户` |

**MyBatis 拦截器链顺序（顺序错误 = 隐蔽 Bug）**：

```
1. TenantLineInnerInterceptor   （租户隔离，必须最外层）
2. DataPermissionInterceptor    （数据权限）
3. PaginationInnerInterceptor   （分页，必须最后 → count 语句也带上前面条件）
4. OptimisticLockerInnerInterceptor（乐观锁）
```

顺序错误的后果：`count` 与 `list` 结果不一致 → 分页页数错乱、总数不准。**必须用集成测试断言 `count` 与 `list` 一致。**

**超管例外**：通过 `@DataScope` 的 `ignoreSuperAdmin` 开关或 `DataScopeStrategy` 策略对象裁决，**不要写死在拦截器里**。

### 7.4 字段级权限

**场景**：同一张表，普通客服看不到用户手机号完整值，财务能看到，超管能看全部。

```java
public class UserVO {
    private String username;

    @FieldPermission(
        visibleFor  = {"ROLE_SUPER_ADMIN", "ROLE_FINANCE"},
        maskStrategy = MaskStrategy.PHONE   // 138****8888
    )
    private String phone;

    @FieldPermission(hiddenFor = {"ROLE_AUDITOR"})
    private String idCardNo;
}
```

- 序列化时读取当前用户权限上下文，按策略隐藏 / 脱敏
- **不侵入业务代码**，加注解即可
- **导出 Excel 与日志复用同一套策略** —— 避免"页面打码但导出泄露"这个经典漏洞

---

## 八、认证与授权（OAuth2.1 / OIDC）

### 8.1 为什么不继续自研 JWT

v1 方案列出"双令牌 + Redis 黑名单 + 令牌版本号"，它**自己承认踢人下线困难**。这套方案的实际终局是：补丁越叠越多（多端登录、SSO、第三方、MFA 都要各写一遍）。

**采用 Spring Authorization Server**，直接获得：

| 能力 | 自研 JWT | Authorization Server |
|---|---|---|
| 令牌吊销（踢人下线） | 需 Redis 黑名单/版本号 | ✅ 内建 |
| 多端管理 | 手写 | ✅ 按 client + 授权会话 |
| SSO（同域/跨域） | 手写 | ✅ OIDC 标准 |
| 第三方联邦登录 | 手写各平台适配 | ✅ 标准 federation |
| MFA / TOTP | 手写 | ✅ 流程可插拔 |
| 服务间调用 | 手写 | ✅ `client_credentials` |

### 8.2 Grant 选型

| 场景 | Grant | 说明 |
|---|---|---|
| **后台管理 SPA（内嵌登录页）** | **自定义 Password Grant**（OAuth2.1 已移除，需自行实现 Provider） | 兼容传统"账号密码 + 验证码"登录体验，同时保留标准令牌体系 |
| 对外第三方接入 | `authorization_code` + PKCE | 标准做法 |
| 服务间 / 开放平台 | `client_credentials` | 机器对机器 |
| 企业 SSO | OIDC federation / SAML | 对接 IdP |

**安全加强方案（推荐）**：**BFF 模式** —— 前端只持 `httpOnly + Secure + SameSite` Cookie，令牌由 BFF 保管，彻底消除 XSS 窃取令牌风险。代价是需要一层 Thin Backend。

### 8.3 令牌与安全策略

```
AccessToken  : JWT，15 min，含 sub / tenantId / authorities
RefreshToken : opaque token，7 d，服务端存储 → 可精确吊销（单点登出）
ID Token     : OIDC，含用户基础信息，供前端初始化
```

- 图形验证码（Redis，可开关，阈值触发）
- 登录失败 N 次锁定（Redis 计数）→ 账号状态由 `User` 聚合裁决
- 敏感操作二次认证（改密、删租户、导出全量数据）
- 记住我 → 延长 RefreshToken 有效期
- 前端 RSA / SM2 传输加密（HTTPS 之外作为纵深防御）
- JWT 签名用 RS256，密钥由 KMS / 环境变量注入，**禁止写死在配置文件**

---

## 九、数据层设计

### 9.1 Flyway 迁移规范

```
admin-bootstrap/src/main/resources/db/migration/
├─ V1.0.0__init_schema.sql             # 全量初始化（仅首次）
├─ V1.0.1__create_iam_tables.sql
├─ V1.0.2__create_org_tables.sql
├─ V1.1.0__add_tenant_quota.sql
├─ V1.1.1__alter_iam_user_add_field_permission.sql
├─ R__view_tenant_statistics.sql       # 可重复执行（视图/存储过程）
└─ afterMigrate.sql                    # 回调：初始化平台级数据（幂等）
```

**铁律**：

1. **已上线的迁移脚本永不修改，只追加**
2. **不写 `down` 脚本** —— 回滚靠新增前向修复脚本，或备份恢复
3. **DDL 与 DML 分离**脚本
4. 存量库用 `baselineOnMigrate` 打基线，之后全部走 Flyway
5. **CI 中必须跑 `flyway validate`**，防止有人偷改历史脚本
6. 禁止在生产手工执行 SQL 后不补脚本

### 9.2 通用字段模板（所有业务表必须包含）

```sql
id           BIGINT       NOT NULL COMMENT '主键（雪花/号段）',
tenant_id    BIGINT       NOT NULL DEFAULT 0 COMMENT '租户ID，0=平台级',
create_by    BIGINT       NULL     COMMENT '创建人',
create_time  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
update_by    BIGINT       NULL     COMMENT '更新人',
update_time  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
del_flag     TINYINT      NOT NULL DEFAULT 0 COMMENT '逻辑删除 0=正常 1=删除',
version      INT          NOT NULL DEFAULT 0 COMMENT '乐观锁版本',
remark       VARCHAR(500) NULL,
PRIMARY KEY (id),
KEY idx_tenant_del (tenant_id, del_flag)
```

**字段类型规范**：

| 场景 | 类型 | 禁止 |
|---|---|---|
| 主键 | `BIGINT` 雪花 / 号段 | **自增**（多租户与分库演进需要全局有序 ID） |
| 金额 | `DECIMAL(18,4)` | `FLOAT` / `DOUBLE` |
| 时间 | `DATETIME(3)`，UTC 存储 | `TIMESTAMP`（2038 问题）、字符串存时间 |
| 布尔 | `TINYINT(1)` | — |
| 枚举 | `TINYINT` + 代码常量 | 存中文描述 |
| JSON | `JSON`（需索引则拆列） | 把关键查询字段塞 JSON |

**其他要求**：**字段注释必填**（代码生成器与元数据驱动依赖它）；表名 `snake_case` 单数 + 明确前缀。

### 9.3 索引规范

| 规则 | 说明 |
|---|---|
| **唯一索引必须带 `tenant_id`** ⚠️ | `UNIQUE KEY uk_tenant_username (tenant_id, username, del_flag)` —— 多租户项目最易漏的一条 |
| 联合索引最左前缀 | 高频查询条件在前，区分度高在前 |
| 索引数量 | 单表 ≤ 5 个（不含主键） |
| 禁止冗余索引 | `(a)` 与 `(a,b)` 同时存在 → 删 `(a)` |
| 覆盖索引 | 高频查询尽量让查询列全在索引中 |
| 禁止函数包裹索引列 | `WHERE DATE(create_time) = ...` 导致索引失效 |
| 外键 | **不建物理外键**，靠应用层与索引保证 |

**CI 静态检查**：Flyway 脚本中检测无主键表、缺索引的高频查询列、大表全表扫描风险。

**逻辑删除与唯一索引的冲突**：`del_flag` 参与联合唯一时无法支持"删除后重建同名"。统一采用：`del_flag` 存 `0` 或**删除时写入主键值**，支持多次删除重建且索引仍有效。

### 9.4 表清单（按上下文分组）

| 上下文 | 表 | 说明 |
|---|---|---|
| **IAM** | `iam_tenant` | 租户（状态机、套餐、配额、到期） |
| | `iam_tenant_package` | 租户套餐定义 |
| | `iam_user` | 用户 |
| | `iam_role` | 角色（dataScope、fieldPolicy、内置标记） |
| | `iam_menu` | 菜单/权限（目录/菜单/按钮） |
| | `iam_user_role` / `iam_role_menu` | 关联 |
| | `iam_role_dept` | 自定义数据范围部门 |
| | `iam_user_credential` | 多因子凭证（TOTP secret、第三方绑定） |
| | `iam_oauth_client` | OAuth2 客户端注册 |
| **Organization** | `org_dept` | 部门（`ancestors` 物化路径） |
| | `org_post` / `iam_user_post` | 岗位 |
| **Platform** | `plt_dict_type` / `plt_dict_data` | 字典（支持租户覆盖） |
| | `plt_config` | 参数配置（平台默认 + 租户覆盖） |
| | `plt_notice` | 通知公告 |
| | `plt_message` | 站内信 |
| | `plt_file` | 文件记录 |
| | `plt_user_preference` | 用户偏好（列设置、工作台布局） |
| | `plt_feature_flag` | Feature Flag |
| | `plt_plugin` | 插件登记 |
| **Audit** | `aud_login_log` | 登录日志（按月分区） |
| | `aud_operate_log` | 操作日志（按月分区） |
| **Tooling** | `tls_table_meta` / `tls_column_meta` | 元数据（代码生成器数据源） |
| | `tls_gen_config` | 生成配置 |
| **Framework** | `event_publication` | Modulith Outbox（自动） |
| | `flyway_schema_history` | 迁移历史（自动） |

### 9.5 缓存策略

| 数据 | 缓存位置 | Key 设计 | 失效方式 |
|---|---|---|---|
| 权限集合 | Redis | `{app}:{tenantId}:perm:{userId}` | 角色变更事件失效 |
| 菜单树 | Redis | `{app}:{tenantId}:menu:{roleKey}` | 菜单/角色变更事件失效 |
| 字典 | Redis + 本地 Caffeine（二级） | `{app}:{tenantId}:dict:{type}` | 主动删除 + 短 TTL 兜底 |
| 租户配置 | Redis | `{app}:{tenantId}:config` | 变更事件失效 |
| 租户元信息 | 本地 Caffeine（TTL 60s） | `{app}:tenant:{code}` | TTL 自然过期 |

**原则**：**缓存不参与正确性，只参与性能**。任何依赖"缓存一定命中"的逻辑都是 Bug。

### 9.6 查询规范

- **分页必须显式** —— 禁止无 `LIMIT` 的全表查询进生产
- **禁止 `SELECT *`** —— 明确列出字段
- 大表深分页用**游标分页**（`WHERE id > ? ORDER BY id LIMIT ?`）而非大 `OFFSET`
- 批量操作分批（每批 ≤ 500 行），避免长事务与大 `IN`
- **`IN` 参数个数上限 1000**
- 禁止在循环中查询（N+1）
- 复杂动态 SQL 用 **XML Mapper**

---

## 十、契约优先流水线

> 这是本方案相对竞品的**最大工程化差异点**。RuoYi / vue-pure-admin 全部是手写请求 + 手写类型。

### 10.1 流水线

```
① 后端写 Controller（springdoc 注解完备）
        ↓
② 构建期产出 openapi.json（springdoc 插件，作为构建产物）
        ↓
③ 发布到制品库 / 提交到 packages/api/openapi.json
        ↓
④ 前端 pnpm gen:api（orval）
        ↓
⑤ 生成内容：
   packages/api/src/generated/
   ├─ types.gen.ts          # 全部 DTO → TS interface
   ├─ schemas.gen.ts        # → Zod schema（运行时校验 + 表单校验来源）
   ├─ endpoints.gen.ts      # → API 函数
   └─ queries.gen.ts        # → TanStack Query hooks（useQuery/useMutation）
        ↓
⑥ 前端业务代码 import { useGetUserPage } from '@admin/api'
```

**收益**：
- 后端改字段 → 前端**编译期直接报错**，而不是上线后才发现 undefined
- 分页/筛选/排序参数全部有提示
- 新接口零手写成本

**`packages/api/src/generated/**` 是生成产物，禁止手工修改。**

### 10.2 CI 破坏性变更门禁

```yaml
- openapi-diff baseline.json current.json --fail-on-breaking
- pnpm gen:api && git diff --exit-code packages/api/src/generated
- tsc --noEmit
```

后端一旦删字段 / 改类型 / 改必填性，**流水线直接失败**，强制前后端沟通。这是"契约"两个字的兑现方式。

### 10.3 前后端职责

**后端**：
- Controller 必须有完备 springdoc 注解（`@Operation` / `@Parameter` / `@Schema`）
- **DTO 必填性必须准确表达**（`@NotNull` / `@NotBlank` 会体现在 OpenAPI 里）
- **字段说明即前端提示文案来源，必须写**
- 枚举值必须有 `description`，否则前端无法生成下拉选项
- 敏感字段（密码、令牌）加 `@Schema(hidden = true)`

**前端**：
- ✅ import 生成产物 / 薄组合 / 用生成的 Zod schema 做校验
- ❌ 手写 `interface UserVO` / 手写 `axios.get('/api/user/list')` / 手写一套与后端不一致的校验规则

### 10.4 破坏性变更处理流程

1. 判断是否真为破坏性：删字段、改类型、改必填性、改枚举取值、改路径 → **是**
2. 优先采用**兼容方案**：新增字段而非修改、废弃而非删除、扩展枚举而非替换
3. 必须破坏时：提前一个 minor 版本发 deprecation 警告 → 与前端同步跑通 `openapi-diff` → 在 CHANGELOG 与迁移指南记录

---

## 十一、前端架构

### 11.1 Monorepo 结构

```
web-all/
├─ pnpm-workspace.yaml
├─ turbo.json                          # 任务编排与缓存
├─ apps/
│  ├─ admin/                           # 主应用
│  │  ├─ src/
│  │  │  ├─ api/                       # 仅业务组合逻辑，基础请求由 @admin/api 生成
│  │  │  ├─ layouts/                   # 布局：侧边栏/头部/标签栏/主区
│  │  │  ├─ router/                    # 静态白名单 + 动态路由装配
│  │  │  ├─ stores/                    # Pinia：仅客户端状态
│  │  │  ├─ views/                     # 页面
│  │  │  └─ main.ts
│  │  ├─ vite.config.ts
│  │  └─ package.json
│  ├─ mobile/                          # 移动端（P4，Vant 4，独立入口）
│  └─ docs/                            # VitePress 文档站
├─ packages/
│  ├─ ui/                              # ★ Pro 组件库（唯一允许 import naive-ui 的地方）
│  │  ├─ src/components/pro-*/         #   Pro 组件（薄封装）
│  │  ├─ src/components/app-*/         #   状态矩阵五件套
│  │  ├─ src/adapters/                 #   内核适配层（naive / vxe / form-create）
│  │  └─ stories/                      #   Histoire stories
│  ├─ api/                             # ★ OpenAPI 生成产物 + 封装
│  ├─ theme/                           # Design Token 唯一来源
│  ├─ utils/                           # 纯函数工具
│  ├─ hooks/                           # 通用组合式函数
│  ├─ directives/                      # v-permission / v-copy / v-debounce
│  ├─ locales/                         # 多语言资源包
│  ├─ plugins/                         # 约定式插件（L1 扩展）
│  └─ config/                          # 共享 tsconfig / vite / oxlint 配置
├─ scripts/
│  ├─ generators/                      # plop 页面/模块生成器
│  └─ codemod/                         # 升级器
├─ examples/                           # 最小可运行示例
└─ package.json
```

**依赖方向**：`apps/*` → `packages/*`。**`packages` 之间不得循环依赖。**

### 11.2 UI 库隔离层（最重要的架构约束）

> **Naive UI 的维护方为社区项目，迭代节奏与生态广度不及 Element Plus。** 隔离层是"UI 库可替换"的保险绳。

| 位置 | 允许 import |
|---|---|
| `packages/ui/**` | ✅ 可以 import `naive-ui` / `vxe-table` / `form-create` |
| `apps/**` 与 `packages/**`（除 `ui`） | ❌ **只能 import `@admin/ui`** |

**后果**：业务代码只写 `import { ProTable, ProForm } from '@admin/ui'`。`@admin/ui` 对外暴露**自有设计系统 API**，内部实现可替换。

**校验**：Oxlint 规则禁止 `apps/**` import 上述库，CI 违规即失败。

### 11.3 组件策略（零自研原则）

#### 社区内核选型矩阵（禁止替换为自研）

| 能力域 | 内核 | 风险 |
|---|---|---|
| 基础元素 | **Naive UI 2.44+** | 低 |
| **复杂表格** | **vxe-table 4.x** | 中 |
| **动态表单** | **@form-create/naive-ui**（v3） | 中 |
| **表单设计器（低代码）** | **form-create-designer-naiveui** | 中 |
| **分片上传 / 断点续传** | **Uppy 4.x + tus-js-client**（后端 tus-java-server） | 中 |
| 富文本 | **Tiptap 3** 或 **wangEditor 5** | 低 |
| Markdown | **md-editor-v3** | 低 |
| 图表 | **ECharts 6 + vue-echarts** | 低 |
| 流程设计器 | **LogicFlow**（审批流）或 **bpmn-js**（BPMN） | 中高 |
| 拖拽排序 | **Sortable.js（vuedraggable）** | 低 |
| 虚拟滚动 | vxe-table 内置 / `@vueuse` `useVirtualList` | 低 |
| 代码编辑器 | **CodeMirror 6** 或 **Monaco** | 低 |
| Cron 编辑器 | **@vue-js-cron** | 低 |
| 图标 | **@vicons**（xicons） | 低 |
| PDF 预览 | **pdfjs-dist / vue-pdf-embed** | 低 |
| 图片裁剪 | **vue-cropper** | 低 |
| 新手引导 | **driver.js** | 低 |
| 命令面板（Cmd+K） | `@vueuse` + **fuse.js**（薄组合 < 200 行） | 低 |
| 快捷键 | **hotkey.js** 或 `@vueuse/useMagicKeys` | 低 |
| 打印 | **vue-plugin-hiprint** | 低 |
| Excel（前端） | **ExcelJS** | 低 |
| 二维码 | **qrcode** | 低 |
| 日期 / 工具 | **dayjs** / **VueUse** | 低 |
| 国际化 | **vue-i18n 11** | 低 |
| Mock | **MSW** + OpenAPI 自动生成 handler | 低 |
| 组件工作台 | **Histoire**（备选 Storybook） | 低 |
| 文档站 | **VitePress** | 低 |
| 脚手架 | **plop** / **hygen** | 低 |
| 代码迁移 | **jscodeshift** | 低 |

**关键依赖必须有备选登记**（Naive UI → Element Plus / TDesign；form-create → 自研薄渲染器 + zod；vxe-table → TanStack Table）。

#### 明确「不封装」清单（避免过度封装）

| 不封装 | 原因 |
|---|---|
| Naive 的按钮 / 输入框 / 下拉 | 直接透传，封装只增加心智负担与类型复杂度 |
| ECharts 的配置项 | `option` 原样透传，不重定义 |
| vxe-table 的编辑能力 | 原样使用，不改交互 |
| dayjs / VueUse | 直接用 |
| 布局栅格 | 直接用 `n-grid` |

**原则：封装厚度 = 只在你需要「加约定」的地方。没有约定需求 → 不封装。**

### 11.4 Pro 组件层

| 组件 | 内核 | 封装职责（只做这些） |
|---|---|---|
| `ProTable` | vxe-table | 搜索区 + 工具栏 + 分页契约 + 字典 + 权限 + 列设置持久化 + 状态矩阵 + 主题桥接 |
| `ProForm` | @form-create/naive-ui | schema 约定 + zod 桥接 + 权限字段 + 布局预设 |
| `ProModal` / `ProDrawer` | Naive | 与 ProForm 组合 + loading + 提交幂等 + 尺寸预设 |
| `ProDescriptions` | Naive | 字段权限 + 字典翻译 + 布局 |
| `ProUpload` | Uppy + tus | UI 外壳 + 业务字段 + 鉴权 + 租户路径 + 预览 |
| `ProEditor` | Tiptap / wangEditor | 图片/附件对接 OSS + 服务端净化 + 主题 |
| `ProTree` | Naive n-tree | 懒加载 + 拖拽 + 搜索 + 部门/菜单预设 |
| `ProSearch` | form-create | 筛选条 + 折叠 + URL query 同步 |
| `ProChart` | ECharts + vue-echarts | 自适应 + 主题联动 + 懒加载 |
| `ProCron` | @vue-js-cron | 中文描述 + 校验 |
| `ProLayout` | Naive 布局原子（**唯一必须自研**） | 侧边/顶栏/混合/分栏 + 标签栏 + 面包屑 + 移动端抽屉 |
| `DictTag` / `DictSelect` | Naive | 字典联动 + 缓存 |
| `IconPicker` | Naive + @vicons | 图标检索 + 预览 |
| `ProTransfer` | Naive | 角色分配 / 权限分配预设 |
| `ProExcel` | ExcelJS | 导入向导（模板下载 + 校验 + 错误行回写） |
| `ProCommand` | VueUse + fuse.js | Cmd+K 命令面板 |
| 状态矩阵 | Naive | `AppLoading` / `AppEmpty` / `AppError` / `AppSkeleton` / `AppDenied` |

#### ProTable 设计（最核心的组件）

```ts
export interface ProTableProps {
  /** 列定义：在 vxe-table column 基础上扩展业务语义 */
  columns: ProColumn[]
  /** 请求函数：直接使用契约生成产物，类型自动对齐后端 PageResult */
  request: (params: ProTableParams) => Promise<PageResult<unknown>>
  search?: ProSearchConfig | false
  toolbar?: (ToolbarKey | CustomToolbar)[]
  rowActions?: ProRowAction[]
  selectable?: boolean
  batchActions?: ProBatchAction[]
  rowKey?: string
  height?: number | 'auto' | 'fill'
}

export interface ProColumn {
  key: string
  title: string
  /** 声明后搜索区自动生成该字段控件 */
  search?: 'input' | 'select' | 'date-range' | 'tree-select' | 'number-range'
  /** 字典编码：自动渲染 DictTag + 搜索区自动变 select + 导出自动翻译 */
  dict?: string
  /** 权限码：无权限时列隐藏 */
  permission?: string
  render?: 'text' | 'dict-tag' | 'link' | 'image' | 'tag' | 'progress' | ((row) => VNode)
  sortable?: boolean
  width?: number | string
  fixed?: 'left' | 'right'
  hidden?: boolean
  disableColumnSetting?: boolean
}
```

**ProTable 封装的 9 件事**：

1. `ProColumn[]` → vxe-table `columns` 映射
2. 搜索区表单（复用 ProForm）→ 参数组装
3. 分页/排序/筛选 → **与后端 `PageResult` 契约严格对齐**
4. `dict` → `DictTag` 渲染 + 搜索区自动换控件 + **导出时自动翻译**（避免"页面显示中文、导出是码值"）
5. `permission` → 列/按钮级隐藏
6. 工具栏标准动作：新增 / 导出 / 刷新 / 列设置 / 密度
7. **列设置持久化**：localStorage + 可同步后端 `plt_user_preference`
8. 状态矩阵：加载 / 空 / 错误 / 无权限 四态统一
9. **主题桥接**：Design Token → vxe-table 主题变量

**ProTable 明确不做**：不重写虚拟滚动、不重写编辑、不重写导出实现、不重写树形展开。

**目标**：典型 CRUD 页面 **≤ 60 行**（RuoYi 同类页面 400~600 行）。核心逻辑 < 800 行。

#### ProForm 与低代码设计器

| 层级 | 做法 | 适用 |
|---|---|---|
| L1 手动 schema | 开发者写 `ProForm` 的 schema 数组 | 常规表单（80% 场景） |
| L2 元数据生成 | 从表结构元数据生成 schema | 支撑域 CRUD |
| L3 可视化设计 | **form-create-designer-naiveui** 拖拽 → rule JSON 存库 → 运行时渲染 | 业务用户自建表单 |

**关键**：L3 的产物是 **rule JSON**（数据），而非代码 —— 这才叫低代码，也才与"好维护"不冲突。

**校验策略（避免双份规则）**：后端 OpenAPI → Zod schema（含长度/格式/必填）→ ProForm 自动推导表单校验规则 → **前后端校验规则天然一致**。后端仍独立校验。

### 11.5 状态管理分工（纠正最常见的架构误用）

| 状态类型 | 归属 | 示例 |
|---|---|---|
| **服务端状态** | **TanStack Query** | 用户列表、字典、菜单、详情、统计 |
| **客户端状态** | **Pinia** | 主题/暗色、布局偏好、标签页、当前用户会话、侧边栏折叠 |
| **URL 状态** | **Vue Router query** | 筛选条件、页码（让列表页可分享、可刷新恢复） |

**为什么重要**：把所有数据塞进 Pinia，意味着要手写缓存失效、并发去重、loading/error 状态、失败重试 —— 每写一个页面重复一遍。

### 11.6 动态路由装配

```ts
// router/guard.ts
router.beforeEach(async (to) => {
  if (whiteList.includes(to.path)) return true
  if (!userStore.token) return { path: '/login', query: { redirect: to.fullPath } }

  if (!permissionStore.isLoaded) {
    const menus = await permissionStore.fetchMenus()      // 后端菜单树
    const routes = buildRoutes(menus)                     // import.meta.glob 查表映射
    routes.forEach(r => router.addRoute(r))
    return { ...to, replace: true }                       // ★ 关键：replace 重进一次
  }
  return true
})
```

- 组件映射表由 `import.meta.glob('@/views/**/*.vue')` 构建，**菜单不硬编码**
- 刷新后从 `sessionStorage` 恢复菜单树，避免闪烁与 404

### 11.7 Naive UI 使用要点与已知坑

| 项 | 约束 |
|---|---|
| **消息/弹窗 API** | `useMessage` / `useDialog` / `useNotification` **必须在 provider 内调用**。脱离 setup 的场景走 `packages/ui` 暴露的全局单例适配器，禁止各处自己 `createDiscreteApi` |
| **主题定制** | 只能通过 `themeOverrides`，值必须来自 `@admin/theme` 的 Token，**禁止硬编码颜色/间距** |
| **暗色模式** | `darkTheme` + Token 双套值自动切换，**不得写 `if (isDark)` 分支调样式** |
| **i18n** | Naive 无内置 i18n → 在 `n-config-provider` 注入 `locale` / `dateLocale` |
| **图标** | 使用 `@vicons/*`；动态菜单图标通过 `import.meta.glob` 动态解析，禁止写死 import 列表 |
| **样式注入** | Naive 是 CSS-in-JS，无 CSS 文件引入；注意首屏 FOUC 与大列表渲染开销（见 §12.3） |
| **表格热路径** | 表格单元格内**禁止放 Naive 组件**（性能），用轻量 `span` + 类名或 vxe-table 自渲染 |

### 11.8 设计系统

```
Design Token（唯一来源：packages/theme/tokens.ts）
  ├─ 基础：色彩（品牌色/语义色/中性色阶）、字号字阶、间距栅格、圆角、阴影、动效曲线
  ├─ 语义：success / warning / danger / info，文本主次，边框，背景层级
  └─ 输出：
      ├─ CSS Variables（供 UnoCSS 与自研组件使用）
      └─ Naive themeOverrides（全量 TS 类型，编译期校验）  ← 选 Naive 的核心理由
```

- 支持品牌色运行时切换（`n-config-provider` 动态注入）
- 暗色模式：Token 双套值，一套代码自动适配
- **禁止硬编码颜色、间距、圆角、字号、阴影**

### 11.9 交互规范：状态矩阵

> **这是「好用」最容易被忽视但感知最明显的部分。** 大量后台系统只做了「成功」一种状态。

| 状态 | 组件 | 规范 |
|---|---|---|
| 加载 | `AppLoading` | 骨架屏优先于转圈；超过 300ms 才显示（防闪烁） |
| 空数据 | `AppEmpty` | 必须带**可操作引导**（"还没有用户，[立即创建]"），禁止只显示"暂无数据" |
| 无搜索结果 | `AppEmpty` variant | 提示"没有匹配结果"，提供"清空筛选" |
| 错误 | `AppError` | 显示可读原因 + 「重试」按钮 + traceId（可复制，便于报障） |
| 无权限 | `AppDenied` | 说明缺哪个权限 + 联系谁，而非白屏 |
| 部分失败 | `AppError` variant | 批量操作部分失败时展示失败清单 + 下载 |
| 提交中 | 按钮 loading + 禁用 | 幂等保护，防重复提交 |
| 危险操作 | 二次确认 | 删除/批量删除/不可逆操作；高风险要求输入名称确认 |
| 撤销 | Toast + 撤销 | 可逆操作提供 5s 撤销窗口（优于二次确认） |

### 11.10 页面模板库

用 **plop / hygen** 生成，命令行一条命令出页面：

```
pnpm gen:page user --type=list        → 列表页（ProTable + 权限 + 路由 + API）
pnpm gen:page user --type=form        → 表单页
pnpm gen:page user --type=detail      → 详情页（ProDescriptions）
pnpm gen:page dept --type=tree-table  → 树表页
pnpm gen:page dash --type=dashboard   → 看板/统计页
```

**模板必须包含**：权限码占位、字典占位、状态矩阵、i18n key、单测骨架、路由注册。

### 11.11 无障碍（a11y）

| 项 | 做法 |
|---|---|
| 静态检查 | `eslint-plugin-vuejs-accessibility`（Oxlint 兼容性需验证，否则用 ESLint 单独跑） |
| 语义化 | 正确使用 landmark；按钮用 button 而非 div |
| 键盘可达 | 所有交互可 Tab 到达，焦点可见，Esc 关闭弹窗 |
| 焦点管理 | 弹窗打开/关闭时焦点归还；路由切换后焦点重置 |
| 颜色对比度 | Token 层保证 WCAG AA（4.5:1），CI 中用 axe 校验 |
| 屏幕阅读器 | 关键操作有 aria-label；表格有 caption |
| 自动化检测 | Playwright + `@axe-core/playwright` 纳入 E2E |

### 11.12 响应式与移动端策略

**明确边界**（避免无底洞）：

| 端 | 策略 |
|---|---|
| PC（≥1280） | 完整功能 |
| 平板（768~1280） | 布局自适应：侧边栏折叠为抽屉；表格横向滚动 + 列设置精简 |
| 移动（<768） | **只做「查看 + 审批 + 简单操作」**，不做完整表单与表格编辑 |
| 移动端组件库 | 若需求重 → 独立接入 **Vant 4**（`apps/mobile` 独立入口）；轻 → 仅响应式适配 |

**关键决策**：**不要试图用一套组件适配三端**，那会毁掉 PC 端体验。移动端是独立入口，复用 `packages/api` 与 `packages/utils`。

### 11.13 首次上手体验

- 内置**示例数据种子**（Flyway `R__seed_demo_data.sql`，仅 dev/test）
- 空数据引导（见状态矩阵）
- 错误信息可操作（"权限不足"→ 显示缺失的权限码）
- 文档站「5 分钟上手」+ 可运行 Demo

### 11.14 构建与性能配置

| 项 | 配置 |
|---|---|
| 引擎 | Vite 8（Rolldown） |
| 分包 | Rolldown `advancedChunks`：`vue` / `naive-ui` / `vxe-table` / `echarts` / `tiptap` 各自独立 chunk |
| 压缩 | `vite-plugin-compression2`（brotli 优先 + gzip） |
| 按需 | Naive UI **无需按需插件**（CSS-in-JS，tree-shaking 天然生效） |
| 体积监控 | `rollup-plugin-visualizer` + CI 体积阈值门禁 |
| Lint | Oxlint + Stylelint + Prettier，husky + lint-staged 守门 |
| 首屏 | 路由懒加载 + 骨架屏 + 关键 CSS 内联 + 路由预取 |

### 11.15 前端红线

1. ❌ `apps/**` 直接 import `naive-ui` / `vxe-table` / `form-create`
2. ❌ 手写接口类型或请求函数
3. ❌ 服务端数据放入 Pinia
4. ❌ 硬编码颜色 / 间距 / 字号（不走 Token）
5. ❌ 菜单硬编码（必须由后端菜单树驱动）
6. ❌ 用 `any` 绕过类型
7. ❌ 在组件内直接 `fetch` 绕过 TanStack Query
8. ❌ 新增 Pro 组件不写 Histoire story 与文档
9. ❌ 表格单元格内放组件库组件
10. ❌ `useMessage` 在 provider 外调用

---

## 十二、性能工程

> **没有数字的"性能高"是自我感觉良好。** 下文所有指标均为硬指标，必须进 CI 门禁。

### 12.1 前端性能预算

| 指标 | 目标 | 门禁 |
|---|---|---|
| 首屏 LCP（4G / 中端机） | **< 1.8s** | Lighthouse CI |
| INP | **< 200ms** | 线上 RUM（web-vitals） |
| CLS | **< 0.1** | Lighthouse CI |
| 首屏 JS（gzip） | **< 250KB** | bundlesize |
| 单个路由 chunk（gzip） | **< 150KB** | bundlesize |
| 表格 1 万行首渲染 | **< 500ms** | Vitest bench |
| 1000 行 × 10 列（含组件单元格） | **< 300ms** | Vitest bench |
| 全量构建耗时 | **< 90s** | CI |
| 增量构建 | **< 15s** | CI |

### 12.2 后端性能预算

| 指标 | 目标 |
|---|---|
| 读接口 P99 | **< 200ms** |
| 写接口 P99 | **< 300ms** |
| 列表分页查询 P95 | **< 150ms** |
| 缓存命中率（字典/权限/配置） | **> 90%** |
| 慢 SQL 阈值 | > 200ms 记录，> 1s 告警 |
| 慢接口阈值 | > 500ms 记录 |
| 单实例读 QPS（虚拟线程） | 压测确定，目标 **≥ 3000** |
| 启动时间 | JVM < 5s / Native < 1s |
| 常规负载内存 | **< 1GB** |
| OTel 采样率 | 生产 10%（错误全采） |

### 12.3 ⚠️ Naive UI 的 CSS-in-JS 开销与降级预案

**风险描述**：Naive UI 基于 `css-render` 在运行时注入样式。风险不在首屏 FOUC，而在：

- 大量组件实例（如 1000 行表格每格放 `n-tag`）→ 样式收集与 DOM 插入开销
- 频繁更新的列表 → 样式重算

**评估与降级预案（按优先级）**：

| 方案 | 说明 |
|---|---|
| **① 结构性规避（首选）** | 表格等高频场景改用 **vxe-table 自渲染**，单元格内用轻量 `span` + 类名而非 Naive 组件。**这是 ProTable 选 vxe 的隐藏收益** |
| ② 静态样式提取 | 构建期提取 Naive 样式为静态 CSS（Naive 支持 SSR 样式提取；SPA 可用插件在构建期预渲染样式表） |
| ③ 关键路径自绘 | 极端热路径（如虚拟滚动单元格）避免使用任何组件库 |
| ④ 实测决策 | **P0 必须跑基准**：1000 行 × 10 列含组件单元格的渲染耗时，作为是否启用 ② 的判断依据 |

**P0 验证任务**：写基准测试页，对比三种方案的渲染耗时与滚动帧率：

| 方案 | 首渲染耗时 | 滚动帧率 | 样式节点数 |
|---|---|---|---|
| `n-data-table` | | | |
| `vxe-table` | | | |
| `vxe-table` + 自绘单元格 | | | |

**用数据决定，不靠猜。**

### 12.4 CI 性能门禁（防止性能腐化）

> 性能优化是一次性的，**门禁才是可持续的**。

```yaml
前端:
  - Lighthouse CI            # LCP / CLS / TBT 阈值门禁
  - size-limit / bundlesize  # 产物体积门禁，超限即失败
  - vitest bench             # 表格渲染基准，对比上次基线（> 15% 退化则失败）
  - Playwright + web-vitals  # 真实浏览器指标

后端:
  - JMH 微基准               # 权限计算、字典翻译等关键算法
  - k6 / Gatling 冒烟压测    # 每夜跑，P95 对比基线，退化则告警
  - 慢 SQL 扫描             # 全表扫描检测、缺失索引检测
```

**新增依赖必须评估产物体积影响**；超出预算需在 PR 中说明理由与替代方案。

### 12.5 首屏秒开策略

| 手段 | 说明 |
|---|---|
| **不做 SSR** | 中台鉴权复杂、SEO 无需求，SSR 收益低而成本高（Node 服务、连接池、部署复杂度） |
| 应用壳预渲染 | 登录页 + 应用壳静态预渲染（Vite SSG），业务页面 SPA + 骨架屏 |
| 骨架屏内联 | 首屏 HTML 内联骨架与关键 CSS，消除白屏 |
| 资源提示 | `<link rel="modulepreload">` + `preconnect`（API/OSS 域名） |
| 路由预取 | 鼠标 hover 菜单时预取对应 chunk |
| 分包 | Rolldown `advancedChunks`（见 §11.14） |
| 压缩 | brotli（优先）+ gzip |
| CDN | 静态资源 CDN；大依赖可选 external |
| 图片 | 懒加载 + WebP/AVIF + 尺寸声明（防 CLS） |
| 字体 | 系统字体栈优先，避免 FOIT |

### 12.6 大表格极限场景

| 场景 | 方案 |
|---|---|
| 10 万行浏览 | vxe-table 虚拟滚动 + **服务端分页**（不要一次拉 10 万行） |
| 万行编辑 | 虚拟滚动 + 编辑模式 + 批量提交 |
| 列数 > 30 | 列设置 + 横向虚拟滚动 + 固定列 |
| 单元格含组件 | **自绘 span 替代组件库**（见 §12.3） |
| 导出 10 万行 | **后端流式导出**（EasyExcel），前端不参与 |
| 树形大数据 | 懒加载 + 服务端树查询 |
| 客户端排序/筛选 | 仅小数据量；大数据走服务端 |

### 12.7 接口瀑布治理

一个页面 8 个接口串行 = 慢。三级方案：

| 级别 | 方案 | 适用 |
|---|---|---|
| **L1（默认）** | 前端**并行请求**（`Promise.all` / TanStack Query `useQueries`） | 默认手段 |
| L2 | **后端批量接口**（`POST /batch`，一次拉多资源） | 首页 / 工作台 |
| L3 | **BFF 聚合**（GraphQL 或自定义聚合层） | 多端复用同一聚合时（谨慎引入） |

**默认只做 L1**，L3 仅在确有需要时引入（BFF 会增加部署与维护成本）。

### 12.8 后端与数据库性能约束

| 项 | 约束 |
|---|---|
| **连接池** | HikariCP `maximum-pool-size` **必须显式限制**；虚拟线程下用 `Semaphore` 保护 DB ⚠️ |
| **虚拟线程** | 禁止用 `synchronized` 包裹阻塞调用（pin 载体线程）→ 用 `ReentrantLock`；线程池包装注意上下文传递 |
| **CPU 密集任务** | 仍用平台线程池（虚拟线程只解决 I/O 阻塞） |
| 缓存 | 多级（Caffeine 秒级 + Redis 分钟级）；防击穿（逻辑过期 + 互斥重建）；防雪崩（TTL 随机抖动） |
| 缓存监控 | 命中率暴露到 Prometheus，**低于阈值告警**（失效风暴是最常见线上事故） |
| 事务 | 短事务；禁止在事务内调用外部 HTTP / 发消息 |
| 大字段 | 列表查询不返回 `TEXT` / `JSON` 大字段 |

**数据库性能演进**：

| 项 | 策略 |
|---|---|
| **分区** | `aud_login_log` / `aud_operate_log` 按 `create_time` RANGE 分区（月） |
| **归档** | N 个月前的日志迁移至归档表/冷存储；提供归档任务与查询入口 |
| **大表演进** | > 1000 万行：分区 / 归档 / 按租户分片（三选一） |
| **读写分离** | 主从；读走从库；**写后立即读走主库**（防主从延迟导致"刚保存就看不到"） |
| **慢查询闭环** | 拦截（>200ms）→ 落库 → 聚合 TopN → 告警 → 治理看板 → 回归验证 |

**压测基线**：k6 / Gatling，场景覆盖登录 / 列表分页 / 详情 / 写入 / 导出 / 权限校验；每夜跑冒烟压测，发布前跑全量；产出**容量规划表**。

---

## 十三、可观测性与测试体系

> v1 在这两块**完全空白**。这是"最佳实践"最硬的硬伤，也是竞品的普遍软肋。

### 13.1 可观测性（三板斧）

| 维度 | 实现 |
|---|---|
| **Tracing** | Micrometer Tracing + OTel Bridge + OTLP Exporter；后端自动埋点（HTTP / JDBC / Redis / 任务），关键路径加 `@Observed` |
| **前端串联** | 前端生成 `traceparent` 请求头 → 后端接续 → **同一 TraceID 贯穿前后端**；Sentry 与后端错误通过 `traceId` 互查 |
| **Metrics** | Actuator + Micrometer → Prometheus：JVM / GC / 虚拟线程数 / 连接池 / 接口 P99 / 业务指标（登录成功率、租户活跃度） |
| **Logging** | JSON 结构化 + MDC（`traceId` / `tenantId` / `userId`）→ Loki / ES；**禁止 System.out** |
| **慢查询** | MyBatis 拦截器记录 >500ms SQL，TopN 落库 + 告警 |
| **慢接口** | 切面统计 P95/P99，TopN 看板 |

**目标**：任何一个线上问题，都能从"用户报错截图"→"前端 Sentry 事件"→"traceId"→"后端全链路 Span"→"具体慢 SQL"一条线走通。

### 13.2 测试金字塔

| 层级 | 工具 | 覆盖目标 | 占比 |
|---|---|---|---|
| **架构测试** | **ArchUnit** + Spring Modulith `ApplicationModules.verify()` | ① domain 不依赖框架 ② 依赖方向正确 ③ 模块边界未被穿透 ④ 命名规范统一 | — |
| **单元测试** | JUnit 5 + AssertJ + Mockito | **领域模型（聚合不变量、状态机、值对象）必须 100%** | 60% |
| **集成测试** | Spring Boot Test + **Testcontainers**（MySQL + Redis + MinIO 真实容器） | Repository、拦截器链、事务、事件、权限 | 30% |
| **契约测试** | `openapi-diff` + 生成客户端编译通过 | 前后端契约一致性 | — |
| **E2E** | Playwright | 登录 → 菜单 → CRUD → 导出 全链路 | 10% |
| **性能测试** | Gatling / k6 | 关键接口压测基线 | — |

**禁止用 H2 替代 MySQL** —— 行为不一致会"测了个假的"。

### 13.3 必测清单（不可妥协）

| # | 必测项 | 说明 |
|---|---|---|
| 1 | **领域聚合的所有不变量与状态流转** | 含**非法流转必须抛异常**（如 `PENDING → CLOSED` 被拒） |
| 2 | **多租户隔离** | A 租户绝对读不到 B 租户数据 —— 含**缓存、文件、异步**三个穿透场景 |
| 3 | **数据权限 + 分页拦截器执行顺序** | 断言 `count` 与 `list` 结果一致 |
| 4 | **幂等注解在并发下的正确性** | 并发调用只成功一次 |
| 5 | **令牌吊销后旧 token 立即失效** | — |
| 6 | **字段权限** | 无权限角色拿不到敏感字段；**导出同样生效** |
| 7 | **水平越权** | 用 A 租户身份按 ID 访问 B 租户资源必须失败 |
| 8 | **乐观锁冲突** | 并发更新抛异常而非静默覆盖 |

### 13.4 架构测试示例

```java
@AnalyzeClasses(packages = "com.xxx.admin")
class ArchitectureTest {

    @ArchTest
    static final ArchRule domain_should_not_depend_on_framework =
        noClasses().that().resideInAPackage("..domain..")
            .should().dependOnClassesThat()
            .resideInAnyPackage("org.springframework..", "com.baomidou..", "jakarta.persistence..");

    @ArchTest
    static final ArchRule domain_should_be_isolated =
        noClasses().that().resideInAPackage("..domain..")
            .should().dependOnClassesThat()
            .resideInAnyPackage("..application..", "..infrastructure..", "..interfaces..");

    @ArchTest
    static final ArchRule application_should_not_depend_on_infrastructure =
        noClasses().that().resideInAPackage("..application..")
            .should().dependOnClassesThat().resideInAPackage("..infrastructure..");
}
```

**按复杂度分级校验**：L1/L2 模块（支撑域 CRUD）不强制 DDD 分层，避免规则被"注释掉"。

### 13.5 测试规范

**命名**：`{被测类}Test` / `{被测类}IT` / `should_{期望行为}_when_{条件}`

**结构（AAA）**：

```java
@Test
void should_reject_activation_when_tenant_is_closed() {
    // Arrange
    Tenant tenant = TenantFixture.aClosedTenant();

    // Act & Assert
    assertThatThrownBy(() -> tenant.activate(Clock.systemUTC()))
        .isInstanceOf(IllegalTenantStateException.class);
}
```

- 一个测试只验证**一个行为**；测试之间**完全独立**
- 用**测试数据构造器 / Fixture**（语义化命名，如 `aClosedTenant()`），禁止在测试里堆 50 行准备代码
- Testcontainers **单例容器**，避免每个测试类重启
- **禁止在测试中依赖生产数据**

**覆盖率**：

| 模块 | 目标 |
|---|---|
| `domain`（聚合 / 值对象 / 领域服务） | **≥ 90%**，核心不变量 **100%** |
| `application` | ≥ 70% |
| `infrastructure` | ≥ 50%（关键：拦截器、仓储实现） |
| 前端 `@admin/ui` | ≥ 70% |

**覆盖率是下限而非目标** —— 不为凑数字写无断言测试。

**前端测试**：Vitest 4 + Vue Test Utils（单元/组件）、Playwright（E2E）、`@axe-core/playwright`（a11y）、Vitest `bench`（性能基准）。

---

## 十四、安全设计

### 14.1 传输与存储

- 全站 HTTPS（HSTS + TLS 1.3）
- 密码 BCrypt（cost ≥ 10）或 Argon2id；**禁止 MD5 / SHA1**
- 敏感字段加密存储（身份证、银行卡）→ `@Encrypted` + 字段级加密转换器
- JWT 签名用 RS256（非对称），密钥由 KMS / 环境变量注入，**禁止写死在配置文件**

### 14.2 防护清单（OWASP 对齐）

| 风险 | 措施 |
|---|---|
| XSS | 输入过滤（请求包装器）+ 输出转义 + 富文本白名单（Tiptap 服务端净化）+ **CSP 响应头** |
| SQL 注入 | 全参数绑定；**MyBatis `${}` 全局禁用**（CI 静态扫描） |
| CSRF | BFF 模式下 Cookie + `SameSite=Strict` + CSRF Token |
| **越权（水平）** | **所有按 ID 查询必须带 `tenant_id` 条件**（拦截器兜底 + 集成测试覆盖） |
| **越权（垂直）** | 后端 `@PreAuthorize` 全覆盖；CI 扫描 Controller 是否漏加权限注解 |
| 文件上传 | 白名单扩展名 + MIME 校验 + **魔数校验** + 重命名 + 存储与访问域分离 + 防目录穿越 |
| 文件下载 | 鉴权 + 签名 URL + 有效期 + 租户路径校验（防越权下载） |
| 反序列化 | 禁用不安全的 Jackson 多态类型 |
| 敏感信息泄露 | 响应脱敏（字段权限）+ 日志脱敏 + 异常栈不返回前端 + **生产禁用 Swagger UI** |
| 暴力破解 | 验证码 + 失败锁定 + IP 限流 + 登录审计告警 |
| 依赖漏洞 | OWASP Dependency-Check / Trivy 纳入 CI |

### 14.3 幂等与限流

```java
@Idempotent(expire = 10, message = "请勿重复提交")   // Redis SETNX + 业务键 + 参数摘要
@RateLimiter(limit = 100, window = 60, dimension = Dimension.TENANT)
@PostMapping("/users")
public R<UserId> create(@Valid @RequestBody CreateUserRequest req) { ... }
```

- **幂等**：只作用于写接口；键 = `租户 + 用户 + 接口 + 参数摘要`
- **限流维度**：IP（防刷）/ 用户（防误用）/ **租户（配额计量，多租户关键）**

---

## 十五、平台治理

> RuoYi 赢在文档与社区，不是代码。要超越，**文档必须是一等公民**。

### 15.1 文档体系

| 类型 | 工具 | 内容 |
|---|---|---|
| **文档站** | **VitePress** | 快速开始（5 分钟跑起来）/ 架构说明 / 开发手册 / 部署手册 / FAQ / 升级指南 / ADR / Cookbook |
| **组件工作台** | **Histoire**（备选 Storybook） | 每个 Pro 组件带可交互 demo + props 表 + 代码示例 |
| **接口文档** | springdoc + Knife4j UI | 在线调试（生产禁用） |
| **架构图** | Spring Modulith 自动生成（C4 图）+ Mermaid | 模块关系、依赖方向 |
| **教程** | 图文 + 视频 | 上手引导、常见场景配方 |
| **示例代码** | `examples/` 目录 | 每个能力一个最小可运行示例 |

**门禁**：新增 Pro 组件必须同时提交 Histoire story + 文档，否则 PR 不通过。

### 15.2 升级机制

要作为"产品"长期维护，升级能力是刚需：

| 项 | 做法 |
|---|---|
| 版本策略 | 语义化版本（SemVer） |
| 变更日志 | `conventional-changelog` 自动生成 CHANGELOG |
| **Breaking Change 政策** | 每个 major 版本集中；提供**自动迁移工具**；提前一个 minor 发 deprecation 警告 |
| **Codemod** | `jscodeshift` 脚本自动改 API 调用 |
| **升级器** | `npx @admin/upgrade`：检测版本 → 应用 codemod → 输出人工待办清单 |
| 迁移指南 | 版本对照 + 前后代码示例 |
| 兼容矩阵 | 明示各版本支持的 JDK / Boot / Node / Vue 范围 |

### 15.3 插件与扩展机制（平台化的分水岭）

**后端扩展点**：

```
├─ Spring Modulith Named Interface：模块对外暴露的稳定 API
├─ Java SPI（META-INF/services）：可选实现（文件存储、短信通道、认证联邦）
└─ Spring 事件：订阅式扩展（审计、通知）
插件表 plt_plugin：id / code / name / version / status / dependencies / config
```

**典型可插拔点**：文件存储实现（S3/OSS/本地）、短信/邮件通道、认证联邦（企业微信/钉钉/LDAP）、消息通道、审批流引擎。

**前端插件**：

| 级别 | 方案 | 适用 |
|---|---|---|
| **L1（默认）** | **约定式插件**：`packages/plugins/*` 运行时注册路由/菜单/组件 | 绝大多数场景 |
| L2 | 动态远程组件（`@originjs/vite-plugin-federation`） | 独立团队独立部署 |
| L3 | 微前端（qiankun / wujie） | 遗留系统集成 |

**默认 L1**，因为它就能满足"业务模块可插拔"，且无额外运维成本。

### 15.4 Feature Flag 与灰度

| 维度 | 支持 |
|---|---|
| 全局开关 | ✅ |
| **按租户** | ✅ **多租户的独特优势**（可先给客户 A 灰度） |
| 按用户 / 按角色 | ✅ |
| 按百分比 | ✅ |

- 存储：`plt_feature_flag` + Redis 缓存 + 变更事件刷新
- 前端 `useFlag('new-dashboard')`；后端 `@FeatureFlag("new-dashboard")`
- 用途：灰度发布、快速回滚（关开关而非回滚版本）、A/B

### 15.5 协作规范

| 项 | 内容 |
|---|---|
| `CODEOWNERS` | `admin-domain/iam` → IAM owner；`packages/ui` → 前端组件 owner；`infrastructure` → 平台 owner |
| PR 模板 | 变更类型、影响面、测试说明、截图 |
| **Review Checklist** | ① 是否加了权限注解？② 是否带 tenant_id？③ 领域层是否被污染？④ 是否有测试？⑤ 是否更新文档？⑥ 是否影响性能预算？ |
| 提交规范 | Conventional Commits + commitlint |
| 分支模型 | trunk-based（短分支 ≤ 3 天）+ 定期 release 分支 |
| 季度架构回顾 | 校准 ADR、清理技术债 |

### 15.6 依赖治理与 EOL 跟踪

| 项 | 做法 |
|---|---|
| 自动升级 | Renovate / Dependabot，分组 + 每周窗口 |
| EOL 台账 | 跟踪 JDK / Spring Boot / Vue / Vite / Naive UI / vxe-table / form-create 的支持周期 |
| **关键依赖备选登记** | Naive UI → 备选 Element Plus / TDesign；form-create → 备选自研薄渲染器 + zod；vxe-table → 备选 TanStack Table |
| 风险评级 | 维护活跃度（最近提交 / issue 响应）+ 版本迭代频率 + 使用方数量 |
| 升级节奏 | 每月小版本，每季度评估大版本 |

> **备选登记很关键**：因为 `@admin/ui` 只对上暴露自有 API，**任何内核库都可替换** —— 这是"零自研 + 好维护"的保险绳。

### 15.7 团队认知分级

| 模式 | 适用场景 | 技术栈 | 上手成本 |
|---|---|---|---|
| **L1 简单模式** | 支撑域 CRUD | 代码生成器 + 事务脚本 + Pro 组件 | **30 分钟** |
| **L2 标准模式** | 常规业务模块 | 契约优先 + Pro 组件 + 页面模板 | **1 天** |
| **L3 严格模式** | 核心域（租户/权限/计费/流程） | DDD 聚合 + 领域事件 + 领域服务 | 需架构组 review |

**文档中明确写出「什么情况用哪个模式」**，并在 PR Checklist 中标注模式。

---

## 十六、部署与 DevOps

### 16.1 环境与拓扑

```
dev     → docker-compose（MySQL / Redis / MinIO / 本地 OTel Collector）
test    → K8s 单节点（GitOps 自动部署）
prod    → K8s 多副本 + MySQL 主从 + Redis 哨兵/集群

Nginx / Ingress
  ├─ /            → 前端静态资源（CDN）
  ├─ /api         → 后端服务（负载均衡）
  └─ /oauth2      → 授权服务器
```

### 16.2 后端容器化

- **Jib** 构建镜像（无需 Dockerfile、无需 Docker daemon、分层缓存友好）
- 基础镜像 `eclipse-temurin:25-jre-alpine` 或 `distroless`
- 多阶段构建 + 非 root 用户运行
- **可选**：GraalVM Native Image（启动 <100ms、内存降 50%+），适用于快速扩缩容场景，代价是构建慢且反射需配置

### 16.3 虚拟线程的正确姿势（JDK 25）

| 配置 | 值 |
|---|---|
| `spring.threads.virtual.enabled` | `true` |
| **禁止** | `synchronized` 包裹阻塞调用（仍会 pin 载体线程）→ 用 `ReentrantLock` |
| **慎用** | `ThreadLocal` → 优先 `ScopedValue`（多租户上下文首选） |
| **连接池** | HikariCP 需限制 `maximum-pool-size`（虚拟线程数量 ≠ 数据库连接数，**不限制会打爆 DB**）← 最大的坑 |
| **限流保护** | 用 `Semaphore` 保护外部依赖（DB / HTTP / Redis），防止无限并发压垮下游 |
| CPU 密集任务 | 仍应使用平台线程池 |

### 16.4 CI/CD 流水线

```
后端 CI：
  checkout → 代码格式(Spotless) → 静态分析(SpotBugs/PMD)
  → 架构测试(ArchUnit/Modulith) → 单元测试
  → 集成测试(Testcontainers) → 依赖漏洞扫描
  → flyway validate → 构建 → 产出 openapi.json
  → openapi-diff（破坏性变更门禁）
  → 镜像构建(Jib) → 推送 → 部署

前端 CI：
  checkout → pnpm install → Oxlint + Stylelint + tsc --noEmit
  → Vitest → pnpm gen:api（校验生成产物与提交一致）
  → Vite build → 体积阈值门禁 → Lighthouse CI → Playwright E2E
  → 制品上传 → 部署

发布策略：蓝绿 / 灰度（**按租户灰度是多租户系统的独特优势**）
回滚：镜像版本回退 + Flyway 前向修复脚本
```

---

## 十七、分期落地路线图

> **重排原则**：把"改造代价随规模指数上升"的事项前置 → 多租户、契约优先、Pro 组件层必须在 P0/P1。
> **每个阶段四个维度同步交付**，避免"先做功能、体验性能后补"（后补 = 永远不补）。

| 阶段 | 组件层 / 功能 | 产品体验层 | 性能工程层 | 平台治理层 |
|---|---|---|---|---|
| **P0 骨架基座**<br>4~6 周 | Naive UI 接入 · Design Token 体系 · 主题桥接 · **vxe-table 选型基准测试** · Monorepo + Maven 多模块 · JDK25/Boot4 + Flyway 基线 · DDD 分层 + ArchUnit 门禁 · **多租户上下文 + 拦截器全链路** · Authorization Server 登录 · 租户/用户/角色/菜单基础 CRUD | `ProLayout` 四种布局 · 登录页 · 页面骨架屏 | **性能预算表落地** · CI 基线（Lighthouse + bundlesize）· **CSS-in-JS 开销实测** | VitePress 文档站骨架 · CODEOWNERS / PR 模板 · ADR 落地 · 依赖 EOL 台账 |
| **P1 权限闭环 + 契约**<br>4~6 周 | **ProTable v1**（搜索+表格+分页+字典+权限+列设置）· **ProForm v1** · 状态矩阵五件套 · 动态菜单/路由 · 按钮权限指令 + 后端鉴权 · 数据权限拦截器 · 字段级权限 · 部门/岗位/字典/配置 · **OpenAPI → orval 生成流水线 + CI 门禁** | 列表页模板 · 表单页模板 · `gen:page` 脚手架 | 首屏预算门禁生效 · 接口 P99 监控 · 缓存命中率监控 | 升级机制（SemVer + CHANGELOG）· Feature Flag · Review Checklist |
| **P2 Pro 组件补齐**<br>5~7 周 | ProUpload / ProEditor / ProModal / ProDrawer / ProDescriptions / ProTree / ProSearch / ProChart / ProCron / IconPicker / ProExcel / ProCommand（**全部薄封装**） | Design Token 文档 · **Histoire 组件工作台** · Cmd+K · 快捷键体系 · 引导（driver.js） | 大表格性能优化 · 接口聚合（L1→L2）· 路由预取 | 插件 SPI 设计 · 模块边界文档 · 贡献指南 |
| **P3 运维 + 质量**<br>4~6 周 | **元数据驱动页面**（生成 schema）· 低代码表单设计器接入 · 操作/登录日志 · 在线用户 · 定时任务中心 · 服务/缓存监控 | 响应式适配（平板）· **a11y 合规 + axe 门禁** · 工作台卡片个性化 · 移动端入口评估 | 压测基线（k6）· 容量规划表 · DB 分区 + 归档 · 慢查询治理闭环 · **性能回归门禁** | 部署手册 · 迁移指南 · 技术债看板 · 季度回顾机制 |
| **P4 生态与差异化**<br>按需 | 流程设计器（LogicFlow + Flowable）· 页面级低代码编排 · 消息中心多通道 · 数据大屏 · 导入导出完善 | 移动端 H5（Vant 4，独立入口）· 多端主题一致性 | 读写分离 · Native Image 评估 · CDN 与边缘缓存 | **插件市场** · 升级器（codemod）· 开发者门户 · 开放平台（API Key / 配额） |
| **P5 AI 原生**<br>按需 | AI 生成 schema / 页面 / 测试 | 智能问答、智能填单、自然语言查数 | — | AI 能力开放为插件 |

**里程碑优先级铁律**：

1. **多租户与契约优先必须 P0/P1 完成** —— 这两项后期改造 = 重写
2. **权限模型定型后不改** —— 改动成本极高
3. **性能预算 P0 建立、P1 开始门禁** —— 拖到 P3 就晚了，代码已经烂了
4. **文档与组件同步交付** —— 组件没有 Histoire story 和文档不算完成
5. **不要一上来拆微服务** —— 模块化单体 + 明确边界，等真有独立扩缩容需求再拆
6. **每个阶段结束做一次「体验走查」** —— 用真实业务流程跑一遍，记录摩擦点

---

## 十八、竞品差异化对照

| 能力 | RuoYi-Vue | RuoYi-Vue-Plus | vue-pure-admin | **本方案** |
|---|---|---|---|---|
| 后端架构 | 包分层 | 包分层（多模块） | — | **DDD 四层 + 限界上下文 + ArchUnit 门禁** |
| 模块化约束 | ❌ | 部分 | — | **Spring Modulith 事务性 Outbox** |
| 多租户 | ❌ | ✅（核心卖点） | — | ✅ + **三种隔离策略可插拔 + 全影响面清单** |
| 认证 | 自研 JWT | Sa-Token | 自研 JWT | **OAuth2.1 / OIDC（标准协议）+ BFF 可选** |
| 权限维度 | 3 维 | 3 维 | 3 维（前端为主） | **4 维（+ 字段级）** |
| 前后端契约 | 手写 | 手写 | 手写 | **OpenAPI → 自动生成 + CI 破坏性变更门禁** |
| 前端工程 | 单应用 | 单应用 | ✅ Monorepo | ✅ **Monorepo + UI 库隔离层可替换** |
| 服务端状态 | Pinia | Pinia | Pinia | **TanStack Query（Pinia 只管客户端状态）** |
| Pro 组件层 | ❌ | ❌ | 部分 | ✅ **17+ 组件（全部二次封装）+ 元数据驱动** |
| 代码生成器 | 生成代码 | 生成代码 | — | **元数据驱动，前端出 schema 而非死代码** |
| 数据库迁移 | ❌ 手工 SQL | ❌ 手工 SQL | — | ✅ **Flyway 版本化 + CI 校验** |
| 测试体系 | ❌ | ❌ | 部分单测 | ✅ **架构 + 单元 + 集成(Testcontainers) + E2E 四层** |
| 可观测 | Actuator | Actuator | ❌ | ✅ **OTel 全链路 + 前端 Sentry TraceID 打通** |
| 性能门禁 | ❌ | ❌ | ❌ | ✅ **量化预算 + CI 回归门禁** |
| 无障碍 a11y | ❌ | ❌ | ❌ | ✅ **axe 自动化检测纳入 E2E** |
| 运行时 | JDK 8/17 | JDK 17/21 | — | ✅ **JDK 25 虚拟线程 + ScopedValue** |
| 前端构建 | Vite 4/5 | Vite 5 | Vite 5/6 | ✅ **Vite 8（Rolldown 统一引擎）** |
| AI 能力 | ❌ | ❌ | ❌ | 🔜 **P5 规划** |

**一句话价值主张**：

> 竞品是「配好权限的后台脚手架」；本方案是「**多租户 SaaS 中台基座**」——
> 契约优先消除前后端摩擦，DDD 保护核心域不腐化，组件零自研降低维护成本，
> 可观测与测试体系让系统能长期演进而不是三年后重写。

**诚实的边界说明**：

- 本方案在**工程化、可维护性、多租户、前后端协作**四个维度可超越竞品，这是实打实的代差
- 但在**视觉质感、交互体验、社区文档、开箱即用**上能否超越，取决于**执行投入**（设计打磨、状态矩阵贯彻、Histoire 文档、a11y），不是架构能自动带来的
- 复杂度叠加是真实风险：DDD + Monorepo + 多租户 + 契约优先 + 组件层是 5 条学习曲线 → 靠 §1.3 的三级模式化解

---

## 十九、架构决策记录（ADR）

项目启动时必须落地以下 ADR 文档（`docs/adr/NNNN-xxx.md`），避免后期扯皮：

| 编号 | 决策 | 状态 |
|---|---|---|
| 0001 | 采用模块化单体而非微服务 | 已定 |
| 0002 | 采用 DDD 分级策略（核心域富模型 / 支撑域事务脚本） | 已定 |
| 0003 | 采用 MyBatis-Plus 而非 JPA | 已定 |
| 0004 | 多租户默认 COLUMN 策略，支持可插拔 | 已定 |
| 0005 | 采用 Spring Authorization Server 而非自研 JWT | 已定 |
| 0006 | 前端 UI 库选 Naive UI，并通过 `packages/ui` 隔离 | 已定 |
| 0007 | 组件零自研：全部二次封装社区成熟方案 | 已定 |
| 0008 | 复杂表格内核用 vxe-table，动态表单内核用 form-create | 已定 |
| 0009 | 契约优先：OpenAPI 为唯一接口定义来源 | 已定 |
| 0010 | 服务端状态归 TanStack Query | 已定 |
| 0011 | 采用 Flyway 管理数据库演进 | 已定 |
| 0012 | JDK 25 虚拟线程启用范围与保护策略 | 待 P0 后实测确定 |
| 0013 | 是否引入 BFF 层 | 待评估 |
| 0014 | 是否使用 GraalVM Native Image | 待评估 |
| 0015 | 移动端是否独立接入 Vant 4 | 待 P3 评估 |

---

## 二十、坑位清单

### 20.1 DDD 相关

| # | 坑 | 规避 |
|---|---|---|
| 1 | **领域层被框架污染**（聚合根加 `@TableName` / `@Service`） | ArchUnit 规则 + CI 强制，破坏即失败 |
| 2 | **聚合根与 PO 混用** | 强制三层对象分离 + MapStruct；Review Checklist |
| 3 | **AppService 里写业务分支 `if`** | 规则进聚合根；Review 时凡见业务 if 一律打回 |
| 4 | **聚合根过大**（把角色、部门塞进 User） | 跨聚合只持 ID 引用 |
| 5 | **DDD 全量套用导致开发效率崩塌** | 严格按 §4.1 分级；支撑域不搞聚合 |
| 6 | **`Instant.now()` 硬编码在领域层** | 注入 `Clock` |
| 7 | **领域事件在事务提交前投递** | Modulith 事务性 Outbox |

### 20.2 多租户相关

| # | 坑 | 规避 |
|---|---|---|
| 8 | **异步线程池丢失租户上下文** → 跨租户数据泄露 | TTL 包装线程池 或 `ScopedValue`；集成测试覆盖 |
| 9 | **唯一索引漏加 `tenant_id`** → 全局撞唯一键 | DDL Review + CI 静态检查脚本 |
| 10 | **定时任务无租户上下文** | 遍历租户执行，`finally` 清理上下文 |
| 11 | **缓存 Key 未带租户前缀** → 串数据 | Key 规范统一封装，禁止裸拼 Redis Key |
| 12 | **文件下载未校验租户路径** → 越权下载 | 签名 URL + 路径校验 |
| 13 | **超管跨租户操作未显式声明** | 提供显式 API，禁止隐式放行 |

### 20.3 权限相关

| # | 坑 | 规避 |
|---|---|---|
| 14 | **MyBatis 拦截器顺序错误**（数据权限在分页之后）→ `count` 与 `list` 不一致 | 固定顺序：租户 → 数据权限 → 分页 → 乐观锁；集成测试断言 |
| 15 | **按钮权限只做前端** → 形同虚设 | 后端 `@PreAuthorize` 全覆盖 + CI 扫描漏加注解的 Controller |
| 16 | **页面打码但导出泄露** | 导出复用同一套字段权限策略 |
| 17 | **按 ID 查询未带租户条件** → 水平越权 | 拦截器兜底 + 集成测试矩阵 |

### 20.4 框架与运行时

| # | 坑 | 规避 |
|---|---|---|
| 18 | **Spring Boot 4 / Security 7 API 变更** | 以官方迁移指南为准，P0 先跑通最小链路 |
| 19 | **MyBatis-Plus 与 Boot 4 适配** ⚠️ | **P0 首日验证项**；不兼容则手写 `SqlSessionFactory` 配置 |
| 20 | **虚拟线程打爆数据库连接池** | HikariCP 显式限制 `maximum-pool-size` + `Semaphore` 限流 |
| 21 | **`synchronized` 在虚拟线程下 pin 载体线程** | 用 `ReentrantLock`；`-Djdk.tracePinnedThreads=full` 排查 |
| 22 | **`ThreadLocal` 在虚拟线程下语义问题** | 优先 `ScopedValue` |
| 23 | **Vite 8 插件生态兼容**（Rolldown 替换 Rollup） | P0 验证插件清单；不兼容时用官方替代或降级该插件 |
| 24 | **Flyway 与历史手工 SQL 冲突** | 存量库用 `baselineOnMigrate` 打基线 |

### 20.5 前端

| # | 坑 | 规避 |
|---|---|---|
| 25 | **Naive UI 的 `useMessage` 必须在 provider 内** | 封装全局适配器 + `app.use` 注入 |
| 26 | **动态路由首次进入 404** | 路由守卫内 `addRoute` 后返回 `{ ...to, replace: true }` |
| 27 | **动态菜单图标不显示** | `import.meta.glob` 建立图标映射表 |
| 28 | **Naive UI CSS-in-JS 首屏 FOUC 与大列表开销** | 见 §12.3 |
| 29 | **业务代码直接 import naive-ui** → 换库等于重写 | Oxlint 规则禁止 `apps/**` import `naive-ui` |
| 30 | **把服务端数据放 Pinia** | 强制 TanStack Query；Review 检查 |
| 31 | **契约生成产物未提交/未校验** | CI 中 `gen:api` 后检查 git diff 为空 |
| 32 | **Naive UI 长期维护风险** | `packages/ui` 隔离层（§11.2）+ 依赖备选登记（§15.6） |
| 33 | **vxe-table 与 Naive 主题体系割裂** | Token → vxe 主题变量桥接层（P0 需实测成本） |

---

## 二十一、P0 验证清单

> **这 8 项是整个架构能否成立的前提。** 任何一项不通过都需要调整方案，不能带着未知风险继续开发。

| # | 验证项 | 不通过的应对 |
|---|---|---|
| 1 | **MyBatis-Plus 与 Spring Boot 4 适配** | 不用 starter，手写 `SqlSessionFactory` 配置 |
| 2 | **vxe-table × Naive UI 主题桥接成本** | 若成本过高，评估 TDesign（原生 vxe 适配更好） |
| 3 | **Naive UI CSS-in-JS 大规模渲染开销** | 启用静态样式提取 / 热路径自绘（见 §12.3） |
| 4 | **`@form-create/naive-ui` 活跃度与兼容性** | 降级为手写 schema + 自研薄渲染器 |
| 5 | **Vite 8 插件生态兼容性** | 替换不兼容插件或降级该插件 |
| 6 | **JDK 25 虚拟线程 + HikariCP 配比** | 显式限制连接池 + `Semaphore` 保护 |
| 7 | **Histoire 活跃度** | 切换到 Storybook |
| 8 | **Oxlint 对 Vue / TS / a11y 规则覆盖** | 保留 ESLint 跑特定规则集 |

**验证要求**：

- 每一项都要**真实执行**，用真实数据，不要凭记忆或推测下结论
- 不确定/未验证的项**必须明确标注**，不要为了让报告好看而假装通过
- 验证过程中的临时脚本与测试文件，**完成后清理**（或放 `examples/` 并说明保留理由）
- 验证结果回写本文档，并更新 [§19 ADR](#十九架构决策记录adr) 中"待评估"的项

### 21.1 验证进展（持续更新）

| # | 验证项 | 状态 | 结论 / 证据 |
|---|---|---|---|
| 1 | MyBatis-Plus 与 Spring Boot 4 适配 | ✅ **已通过** | 官方提供 `mybatis-plus-spring-boot4-starter:3.5.17`（2026-07-08 发布）。**无需**降级为手写 `SqlSessionFactory`，风险解除 |
| 5 | 相关生态版本核实 | ✅ **部分通过** | Spring Boot `4.0.8`（最新 4.0.x）、springdoc-openapi `3.1.1`、Vite `8.3.0`、Vue `3.5.42`、naive-ui `2.45.3`、Pinia `4.x`、Vue Router `5.x`、vue-tsc `3.3.x` 均已确认可用 |
| 2 | vxe-table × Naive UI 主题桥接成本 | ⏳ 待验证 | P0 骨架暂用原生表格渲染，内核替换已封装在 `ProTable` 内部 |
| 3 | Naive UI CSS-in-JS 大规模渲染开销 | ⏳ 待验证 | **已发现关联问题**：骨架实测首屏 JS gzip `280KB`（超出 §12.1 的 250KB 预算），主因是 `app.use(naive)` 全量注册。这是首个明确的性能优化任务 |
| 4 | `@form-create/naive-ui` 活跃度与兼容性 | ⏳ 待验证 | — |
| 6 | JDK 25 虚拟线程 + HikariCP 配比 | ⏳ 待验证 | 骨架已在 `application.yml` 中显式限制 `maximum-pool-size: 20`（未压测） |
| 7 | Histoire 活跃度 | ⏳ 待验证 | — |
| 8 | Oxlint 对 Vue / TS / a11y 规则覆盖 | 🟡 **部分通过** | ✅ 已验证：`no-restricted-imports` 能精确拦截 UI 库直引并输出定制提示（`eslint` 插件需显式启用；**配置文件必须放仓库根**，否则 overrides 的 glob 相对配置文件目录解析而失效）。⏳ a11y 规则覆盖待验证 |
| — | 架构门禁有效性 | ✅ **已通过** | ArchUnit 10 条规则全部生效，并已用**反向探针**验证：在领域层故意加 `@Component` 后构建立即失败并精确指出违规类 |

### 21.2 实测踩到的坑（均已解决，务必复用这些结论）

P0 骨架搭建过程中实际遇到并修复的问题。**这些是只有真跑起来才会暴露的信息，比任何文档推断都可靠。**

| # | 坑 | 现象 | 解决方式 |
|---|---|---|---|
| 1 | **Spring Boot 4.0 的 BOM 不再托管 Spring Modulith 与 Testcontainers** | 构建报 `dependencies.dependency.version is missing`，POM 直接无法解析 | 父 POM 显式 `import` 两个 BOM：`spring-modulith-bom:2.0.8`（与 Boot 4.0 同线）、`testcontainers-bom:2.0.5`（取自 `spring-boot-testcontainers:4.0.8` 的 POM，保证与 `@ServiceConnection` 对齐） |
| 2 | **Testcontainers 2.0 给所有模块加了 `testcontainers-` 前缀** | `org.testcontainers:mysql` 找不到 | 更名为 `testcontainers-mysql` / `testcontainers-junit-jupiter`（这是 2.0 的破坏性改名，1.x 写法全部失效） |
| 3 | **MyBatis-Plus 3.5.9+ 把 SQL 解析能力拆到独立模块** | `PaginationInnerInterceptor` / `TenantLineInnerInterceptor` / `TenantLineHandler` 全部「找不到符号」，`net.sf.jsqlparser` 包不存在 | 显式引入 `com.baomidou:mybatis-plus-jsqlparser`。**只引 starter 是不够的** |
| 4 | **`ErrorCode` 接口用 record 风格访问器，Lombok `@Getter` 无法满足** | `CommonErrorCode 不是抽象的, 并且未覆盖 message()` | 手写 `code()` / `message()`。⚠️ 注意 `httpStatus()` 是接口 default 方法，靠 Lombok 的 `getHttpStatus()` 会**静默返回 200**，导致 401/403 失效 —— 这类问题不报编译错，只会让认证语义出错 |
| 5 | **ArchUnit 的 `DO_NOT_INCLUDE_JARS` 会过滤掉自己的模块** | 10 条规则全部「failed to check any classes」而失败 —— 看起来像架构违规，实际是导入范围错了 | 在「聚合入口模块」跑架构测试时**不要**用该选项；范围收敛靠 `importPackages("com.webadmin")` 完成 |
| 6 | **springdoc 契约插件绑在默认生命周期上会污染构建** | 每次 `mvn install` 都因连不上 `localhost:8080` 报 `ConnectException` | 移入 `-Popenapi` profile（契约生成需要先启动应用，属于显式发布流程） |
| 7 | **record 的非规范构造器必须委托给规范构造器** | `TenantId` 的私有构造器直接赋值字段 → 编译错误 | 改为「规范构造器管底线（非负）+ 工厂方法管语义（业务创建要求正数 / 还原允许 0）」 |
| 8 | **Oxlint 配置文件不能放在子目录** | `--config packages/config/oxlint.json` 时 overrides 的 glob 匹配不上，规则静默失效 | 配置放仓库根 `.oxlintrc.json`（自动发现，glob 相对根解析） |
| 9 | **基础设施层实现应用层端口需要依赖 `admin-application`** | 编译缺失包 | 这是**合法方向**（Adapter 必须看得见 Port）。禁止的是反向：`application` 不得依赖 `infrastructure`，由 ArchUnit 强制 |
| 10 | **PowerShell 传 `-D` 参数需加引号** | `mvn ... -Djava.version=21` 被拆成 `Unknown lifecycle phase ".version=21"` | 写成 `"-Djava.version=21"` |
| 11 | **系统 PATH 中的 Maven 路径被截断成无效路径** | `mvn` 命令不识别，看起来像"没装 Maven"。实际系统 PATH 里那条是 `rogram Files\Apache\apache-maven-3.9.16\bin`（43 字符，**开头 `C:\P` 丢失**），`Test-Path` 为 False，Windows 直接跳过 | 修正该条目为 `C:\Program Files\Apache\apache-maven-3.9.16\bin`。⚠️ **教训**：检查 PATH 时必须用 `Test-Path` 验证路径有效性，不能用 `-match 'apache-maven'` 字符串匹配 —— 后者会被坏记录骗过（本次就误判过一次"已包含"） |
| 12 | **多 JDK 环境下用错版本** | 机器上并存 JDK 17/21/25，`JAVA_HOME` 指向 21，而项目基线是 25 | 本机 JDK 25 位于 `C:\Java\jdk-25`（已验证可完整构建 + 33 测试通过）。固定版本的两种方式：① 改 `JAVA_HOME`（简单，适合单版本需求）② Maven Toolchains（适合多项目各需不同 JDK）。本项目选 ① |
| 13 | **Spring Boot 4.0 已换用 Jackson 3** | 配置 `spring.jackson.serialization.write-dates-as-timestamps` 导致**启动失败**：`No enum constant tools.jackson.databind.SerializationFeature.write-dates-as-timestamps` | 包名 `com.fasterxml.jackson` → `tools.jackson`；该 feature 在 Jackson 3 中**已被移除**（默认就输出 ISO-8601）。ArchUnit 的禁用包清单同步加入 `tools.jackson..`，避免换版本时漏拦 |
| 14 | **Spring Boot 4.0 把各技术的自动配置拆成了独立模块** | Flyway 完全不工作：应用正常启动、数据源连通、`V1.0.0` 脚本确实在 jar 内，但**日志里没有任何 Flyway 输出**，表未创建，接口报「Table doesn't exist」 | 需显式引入 `org.springframework.boot:spring-boot-flyway`。**排查口诀：grep 启动日志有没有 "Flyway" 字样 —— 完全没有即说明自动配置压根没生效**，而不要去怀疑脚本路径。同类还有 `spring-boot-liquibase` / `spring-boot-jdbc` 等 |
| 15 | **Flyway 的 `baselineVersion` 默认值 1 会「静默跳过」`V1.0.0`** | 迁移日志一切正常，但表就是不存在。查 `flyway_schema_history` 只有一条 `BASELINE`，没有 `V1.0.0` | Flyway 的版本比较**忽略尾随的 0**，即 `1` == `1.0` == `1.0.0`，于是 `V1.0.0` 被判为「已被基线覆盖」而跳过。**必须设 `baseline-version: 0`**。⚠️ 触发条件极苛刻：仅当【库非空】且【尚无迁移历史】时出现。本例正是被 Modulith 先行创建的 `event_publication` 表触发 —— **干净库上首跑不会复现，本地能过、CI 上却挂** |
| 16 | **Modulith 的 JDBC Outbox 需要额外引入序列化模块** | 启动失败：`required a bean of type 'org.springframework.modulith.events.core.EventSerializer' that could not be found` | `spring-modulith-events-jdbc` 只负责「存」，事件载荷的序列化由 `spring-modulith-events-jackson` 提供，必须一并引入 |
| 17 | **`@ApplicationModuleListener` 不在 `spring-modulith-api`** | 编译报「找不到符号」 | 实际位于 `spring-modulith-events-api`，且包名是 `org.springframework.modulith.events`（**不是** `org.springframework.modulith`）。仅凭名字极易引错模块 |
| 18 | **MySQL 会话时区导致「同一列混两种时间基准」** | DB 侧的 `CURRENT_TIMESTAMP` / `ON UPDATE CURRENT_TIMESTAMP` 写入**本地时间**（本机 UTC+8），而应用侧通过 `Instant` 写入 **UTC**，同一列两种基准且不报错 | JDBC URL 加 `connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true`（Connector/J 8.0.23+ 已用 `connectionTimeZone` 取代旧的 `serverTimezone`）。✅ 已验证：种子数据落库为 `05:42` 而本地时间为 `13:42`，读回为 `...Z`，前后一致 |
| 19 | **框架自管的表可能与业务表时区不一致** | `event_publication.publication_date` 写的是本地墙上时间（13:45），而业务表是 UTC（05:45） | Modulith 自管表使用 JVM 默认时区。P0 阶段可接受；若要彻底统一，需设 `-Duser.timezone=UTC` 或对框架表单独处理。**结论：不要假设所有表都用同一时区基准** |
| 20 | **默认端口"可用"不代表能安全复用 —— 别的项目已经占着** | 早期 `docker compose up -d` 直接报连不上 Docker API；后来引擎可用时又面临端口全被占 | 本机同时跑着另一个项目（`E:\front-backend-web\ecommerce`）的容器：**6379 是它的 redis**、3306 是原生 MySQL、6380 是原生 Redis。本项目的 compose 因此采用**独立项目名 + 独立端口 + 独立数据卷**（本项目 Redis → `6381`，容器名 `webadmin-redis`）。<br>⚠️ **教训**：与其他项目共用一个 Redis 会导致键空间互相覆盖、`FLUSHDB` 误伤他人数据、生命周期互相牵扯，且故障极难定位。<br>排查手法：`docker ps` + `docker compose ls` 看清本机有哪些容器、各属哪个项目 —— 这一步应当在使用任何"默认端口服务"之前做 |
| 21 | **会话早期测得的结论被当成永久事实** | 早期实测本机 Redis 为 `3.0.504`（2016 年微软移植版），据此给出"必须升级 Redis"的建议；复测时实际已是 `8.10.1`（容器）/ `8.8.0`（原生） | 环境在本次会话期间被改动过（服务被升级/重启）。**教训：环境类结论在使用前必须重新核实**，不能用几分钟前的测量当依据 —— 本次因此给出了错误建议，属实质性判断失误 |

### 21.3 P0 端到端验证结果（已实测跑通）

以下链路已**在真实环境中执行并验证**，不再是「仅通过编译」：

| 链路 | 验证方式 | 结果 |
|---|---|---|
| 应用启动 | JDK 25 + `java -jar` | ✅ 3.3 秒启动，Tomcat 监听 8080 |
| Flyway 迁移 | `flyway_schema_history` 记录 | ✅ `BASELINE(0)` + `V1.0.0 init database` success=1 |
| 表结构创建 | `SHOW TABLES` | ✅ `iam_tenant`、`event_publication`、`flyway_schema_history` |
| MyBatis 拦截器链 | 分页查询 `count` 与 `list` 口径 | ✅ 返回 total=1，与列表一致 |
| 多租户上下文 | 带 `X-Tenant-Id` 请求 | ✅ 正常查询；缺失时按设计走异常路径 |
| 雪花 ID | 创建接口返回 | ✅ `2099373406374273025`（19 位，趋势递增） |
| 领域不变量 | 创建后状态 | ✅ 返回 `PENDING`（需显式激活才可用） |
| 唯一性校验 | 重复编码创建 | ✅ `code=30002` 租户编码已存在 |
| 参数校验 | 非法编码格式 | ✅ `code=10000`，并合并多条校验消息 |
| 全局异常处理 | 查询不存在的租户 | ✅ `code=10002` + HTTP 200（业务错误走 code，仅认证类用 401/403） |
| 审计字段填充 | 查表 | ✅ `create_time` / `update_time` 均写入 UTC |
| **事务性 Outbox** | 创建租户后查 `event_publication` | ✅ 有记录，`completion_date` 晚于 `publication_date` 36ms，监听器日志在 `task-1` 线程输出（异步投递生效） |
| 中文 UTF-8 | 接口响应原文 | ✅ `成功` / `平台默认租户` / `企业版` 均正确 |

**尚未验证**（诚实标注）：数据权限拦截器（P1 才实现）、字段级权限、缓存与 Redis 实际使用、认证与权限注解、vxe-table 内核、首屏性能预算。

---

## 附录：目录结构总览

```
web-all/
├─ 后台管理系统功能设计方案.md          # ★ 本文档：唯一权威设计文档
├─ CODEBUDDY.md                        # AI 协作轻量入口
├─ .codebuddy/
│  ├─ rules/                           # 项目规则（11 条）
│  └─ commands/                        # 自定义命令（8 个）
├─ docs/
│  ├─ adr/                             # 架构决策记录
│  ├─ guide/                           # 快速开始 / 开发手册 / 部署手册
│  ├─ architecture/                    # 架构说明 + 自动生成模块图
│  ├─ cookbook/                        # 场景配方
│  └─ changelog/                       # 变更日志 + 迁移指南
├─ apps/
│  ├─ admin/                           # 前端主应用
│  └─ mobile/                          # 移动端（P4，Vant 4）
├─ packages/
│  ├─ ui/                              # ★ 唯一 import 第三方 UI 库的地方
│  ├─ api/                             # ★ 契约生成产物
│  ├─ theme/                           # Design Token 唯一来源
│  ├─ plugins/                         # 约定式插件
│  └─ utils/ hooks/ directives/ locales/ config/
├─ scripts/
│  ├─ generators/                      # plop 页面/模块生成器
│  └─ codemod/                         # 升级器
├─ examples/                           # 最小可运行示例
└─ server/                             # 后端 Maven 多模块
   ├─ admin-common/
   ├─ admin-domain/                    # ★ 零框架依赖
   ├─ admin-application/
   ├─ admin-infrastructure/
   ├─ admin-interfaces/
   ├─ admin-bootstrap/                 # main + Flyway 脚本
   └─ admin-test-support/
```

---

**文档结束 · v4.0 权威版**

